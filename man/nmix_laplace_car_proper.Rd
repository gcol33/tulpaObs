% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmix_laplace_spatial.R
\name{nmix_laplace_car_proper}
\alias{nmix_laplace_car_proper}
\title{Proper CAR Royle (2004) N-mixture model via nested Laplace}
\usage{
nmix_laplace_car_proper(
  y,
  site_idx,
  map_site_to_unit,
  X_lambda,
  X_p,
  adj_row_ptr,
  adj_col_idx,
  n_neighbors,
  n_spatial,
  tau_grid = NULL,
  rho_grid = NULL,
  mixture = c("P", "NB"),
  r_grid = NULL,
  beta_lambda_init = NULL,
  beta_p_init = NULL,
  z_init = NULL,
  K_max = NULL,
  max_iter = 100L,
  tol = 1e-06,
  verbose = FALSE
)
}
\arguments{
\item{y}{Integer vector of observed counts (long form, one entry per visit).}

\item{site_idx}{Integer vector, 1-based site index for each visit, same
length as \code{y}.}

\item{map_site_to_unit}{Integer vector of length \code{n_sites}, 1-based spatial
unit index for each site. Sites can share spatial units; the data
contribution to \code{z[u]} aggregates across all sites that map to \code{u}.}

\item{X_lambda}{Numeric matrix \verb{[n_sites x p_lambda]} of abundance covariates.}

\item{X_p}{Numeric matrix \verb{[n_obs x p_p]} of detection covariates.}

\item{adj_row_ptr, adj_col_idx, n_neighbors}{CSR adjacency for the ICAR graph
on the \code{n_spatial} units. \code{adj_row_ptr} has length \code{n_spatial + 1},
\code{adj_col_idx} lists 0-based neighbours, and \code{n_neighbors[s]} is the row
degree of unit s.}

\item{n_spatial}{Number of spatial units.}

\item{tau_grid}{Optional numeric vector of \eqn{\tau} grid points
(defaults to \code{exp(seq(log(0.3), log(30), length.out = 7L))}).}

\item{rho_grid}{Optional numeric vector of \eqn{\rho} grid points in
the valid eigenvalue interval. Defaults to a 5-point grid in
\eqn{(0, 1)} -- callers that want eigenvalue-derived bounds should
compute them via tulpa::spatial_car_proper(adjacency)\code{$}rho_bounds and
pass an explicit grid in that interval.}

\item{mixture}{Abundance mixing distribution \code{"P"} (default) or \code{"NB"}; under
\code{"NB"} the NB size \eqn{r} is integrated as an additional outer grid axis.}

\item{r_grid}{Optional NB size grid (NB only); see \code{\link[=nmix_laplace_icar]{nmix_laplace_icar()}}.}

\item{beta_lambda_init}{Optional warm start; default \code{c(log(mean(y)+0.1), 0, ...)}.}

\item{beta_p_init}{Optional warm start; default \code{rep(0, p_p)}.}

\item{z_init}{Optional warm start for the spatial field; default zeros.}

\item{K_max}{Truncation for the per-site marginal sum over N. Defaults to
\code{max(y) + 100}. Returned \code{boundary_max} flags any grid point whose worst
site puts non-trivial mass on \code{K_max} -- raise \code{K_max} if it exceeds 1e-4.}

\item{max_iter, tol}{Inner Newton iteration budget and gradient-norm tol.}

\item{verbose}{Print per-iteration and per-grid-point progress.}
}
\value{
A list of class \code{nmix_spatial_fit}:
\itemize{
\item \code{theta_grid} -- \verb{[n_grid x 2]} matrix of (tau, rho) per grid point
\item \code{tau_grid}, \code{rho_grid} -- input axes
\item \code{log_det_Q_rho} -- precomputed log determinants per rho
\item \code{log_marginal}, \code{weights}, \code{tau_mean}, \code{tau_sd}, \code{rho_mean}, \code{rho_sd}
-- as in \code{\link[=nmix_laplace_icar]{nmix_laplace_icar()}} but with the rho marginal added
\item other diagnostic fields and named coefficient means as in ICAR
}
}
\description{
Nested-Laplace fit of the spatial N-mixture model with a proper conditional
autoregressive prior on the abundance-arm spatial offset:
\deqn{N_i \sim \mathrm{Poisson}(\lambda_i), \qquad
      y_{ij} | N_i \sim \mathrm{Binomial}(N_i, p_{ij}),}
\eqn{\log \lambda_i = X_\lambda^{(i)} \beta_\lambda + z_{u(i)}}, where
\eqn{z \mid \tau, \rho \sim \mathrm{N}(0, [\tau (D - \rho W)]^{-1})}.
Both hyperparameters are integrated over an outer 2D grid; the inner
Newton step shares the kernel with \code{\link[=nmix_laplace_icar]{nmix_laplace_icar()}} (ICAR is
the \eqn{\rho = 1} limit).

Unlike the ICAR fit, no sum-to-zero centering is applied -- \eqn{Q(\rho)}
is full rank for \eqn{\rho < 1}. The per-rho \eqn{\log |Q(\rho)|} is
precomputed once via a dense Cholesky on the \eqn{n_{\mathrm{spatial}}
\times n_{\mathrm{spatial}}} precision matrix.
}
\references{
Cressie, N. (1993). Statistics for Spatial Data. Wiley.
Rue, H., Held, L. (2005). Gaussian Markov Random Fields. CRC.
}

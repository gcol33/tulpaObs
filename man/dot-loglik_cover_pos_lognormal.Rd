% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sla_cover_hurdle.R
\name{.loglik_cover_pos_lognormal}
\alias{.loglik_cover_pos_lognormal}
\title{Lognormal-arm log-likelihood (positive subset only)}
\usage{
.loglik_cover_pos_lognormal(beta_pos, sigma_pos, enc)
}
\arguments{
\item{beta_pos}{Length-\code{p_pos} numeric coefficient vector.}

\item{sigma_pos}{Positive scalar (held fixed at the fitted value).}

\item{enc}{Encoded data list from \code{encode_cover_hurdle()}.}
}
\value{
Scalar log-likelihood.
}
\description{
Note: the encoded \code{enc$pos_data$y} is already on the log scale (i.e.
\code{log(cover[occur == 1])}; see \code{encode_cover_hurdle()}). On the log
scale the lognormal density reduces to a Gaussian on \verb{log y} with
mean \verb{X beta_pos} and SD \code{sigma_pos}. The Jacobian \verb{- log y} is
constant in \code{beta_pos} and so does not affect the third derivative;
we drop it. (For a finite-difference d3 along the beta_pos direction,
beta-independent terms cancel.)
}
\keyword{internal}

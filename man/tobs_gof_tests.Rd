% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{tobs_gof_tests}
\alias{tobs_gof_tests}
\alias{test_dispersion.tobs_fit}
\alias{test_zero_inflation.tobs_fit}
\alias{test_outliers.tobs_fit}
\title{Goodness-of-fit tests for a tobs fit}
\usage{
\method{test_dispersion}{tobs_fit}(
  object,
  n.samples = 250,
  nsim = NULL,
  seed = NULL,
  observed = NULL,
  alternative = c("greater", "two.sided", "less"),
  ...
)

\method{test_zero_inflation}{tobs_fit}(
  object,
  n.samples = 250,
  nsim = NULL,
  seed = NULL,
  observed = NULL,
  ...
)

\method{test_outliers}{tobs_fit}(
  object,
  n.samples = 250,
  nsim = NULL,
  seed = NULL,
  observed = NULL,
  ...
)
}
\arguments{
\item{object}{A fitted \code{tobs_fit}.}

\item{n.samples}{Number of posterior-predictive replicates to simulate.}

\item{nsim}{The same budget under the name the \pkg{tulpa} default methods
use; overrides \code{n.samples} when given.}

\item{seed}{Optional random seed for the replicates; the caller's RNG stream
is restored afterwards.}

\item{observed}{Must be \code{NULL}: a tobs fit scores the response it was
fitted to. Accepted so a call spelled for the tulpa default reaches the
method.}

\item{alternative}{Tail of the dispersion p-value: \code{"greater"} (default,
over-dispersion), \code{"two.sided"} or \code{"less"}.}

\item{...}{Must be empty; an unrecognised argument is an error.}
}
\value{
A list with the observed statistic (\code{observed}), its
posterior-predictive expectation (\code{expected}), their ratio (\code{ratio})
and a tail p-value (\code{p.value}). The names are the same on every
family, so a caller can read the statistic without branching on the
model type.
}
\description{
The \pkg{tulpa} goodness-of-fit generics for \code{tobs_fit}: dispersion,
zero-inflation, and outlier counts compared against the fitted model's
simulated replicates. For the Kolmogorov-Smirnov uniformity test, hand
\code{\link[=pit_residuals]{pit_residuals()}} to \code{\link[tulpa:test_uniformity]{tulpa::test_uniformity()}}, which takes a PIT vector
directly.
}

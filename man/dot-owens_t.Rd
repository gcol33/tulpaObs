% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplified_laplace.R
\name{.owens_t}
\alias{.owens_t}
\title{Owen's T function via numerical quadrature}
\usage{
.owens_t(h, a)
}
\description{
T(h, a) = (1 / (2*pi)) * integral_0^a exp(-h^2 (1 + x^2) / 2) / (1 + x^2) dx
}
\details{
Used in the skew-normal CDF:
Phi_SN(x; xi, omega, alpha) = Phi(z) - 2 * T(z, alpha), z = (x - xi) / omega.

Pure quadrature implementation. Closed-form series (Patefield-Tandy)
is faster but more error-prone; we only call this from \code{.sn_cdf()} which
is itself only called from \code{.sn_quantile()} (rare path, used for CIs
when SN representation kicks in). Speed is not the bottleneck.

Identities used to handle endpoint cases:
T(0, a) = atan(a) / (2*pi)
T(h, 0) = 0
T(h, a) = sign(a) * T(h, |a|)   (antisymmetric in a)
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occu_cover_batch.R
\name{occu_cover_inputs}
\alias{occu_cover_inputs}
\title{Build occu_cover() model inputs from a long, plot-level frame}
\usage{
occu_cover_inputs(
  data,
  site,
  visit,
  response,
  y_pos,
  occ.covs = NULL,
  det.covs = NULL,
  coords = NULL,
  compact = TRUE,
  positive = "beta"
)
}
\arguments{
\item{data}{A long / plot-level data frame: one row per site-visit.}

\item{site, visit, response, y_pos}{Column names: the site identifier, the
visit / replicate, the 0/1 detection response, and the continuous cover
response (used only where \code{response == 1}). The accepted range of \code{y_pos}
follows \code{positive}.}

\item{occ.covs}{Optional character vector of site-level covariate columns.
The default (\code{NULL}) keeps the full first-row-per-site frame and lets the
occurrence / detection / positive formulas select what they reference.}

\item{det.covs}{Optional character vector of visit-level covariate columns.}

\item{coords}{Optional length-2 character vector of coordinate columns.}

\item{compact}{Logical (default \code{TRUE}). Build the compact (ragged) arms the
joint nested-Laplace \code{occu_cover()} engine consumes (no per-site visit
cap), or the dense \verb{[n_sites x max_visits]} grid.}

\item{positive}{The cover-arm distribution, matching \code{\link[=occu_cover]{occu_cover()}}: \code{"beta"}
(default) stores \code{y_pos} as a proportion in \verb{[0, 1]} (\code{tobs_data()} type
\code{"cover"}); \code{"lognormal"} / \code{"gamma"} store it as a positive real \verb{(0, Inf)}
(type \code{"positive"}, validated non-negative with no upper bound). Pick the
value you pass to \code{occu_cover(response = )}.}
}
\value{
A list with \code{y}, \code{y_pos}, \code{visits}, \code{site_data}, \code{coords},
\code{n_sites}, \code{max_visits}, \code{n_visits}, and \code{compact}.
}
\description{
Assemble the paired occurrence / cover response arms, the visit-level
covariate grid, and the site-level design frame that an \code{\link[=occu_cover]{occu_cover()}} fit
consumes, from one long (one row per site-visit / plot) data frame. This is
the same construction \code{\link[=tobs]{tobs()}} runs internally when a single \code{occu_cover()}
fit is handed a long frame; call it directly when you want to inspect the
arms before fitting, or pass them on as \verb{y = }, \verb{y_pos = }, \verb{visits = },
\verb{data = } to \code{\link[=tobs]{tobs()}}.

The occurrence and cover arms are pivoted onto one shared \verb{(sites, visits)}
grid, so they align row-for-row, and the site-level design is the first row
per site. The cover arm is positive-only (meaningful where the species is
detected): in the dense grid absent / unsampled cover cells are set to \code{0},
and in the compact (ragged) carrier they are \code{NA} in the values and never
read by the joint engine.
}
\seealso{
\code{\link[=tobs]{tobs()}}, \code{\link[=tobs_data]{tobs_data()}}.
}

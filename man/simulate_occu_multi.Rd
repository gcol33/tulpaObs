% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occu_multi.R
\name{simulate_occu_multi}
\alias{simulate_occu_multi}
\title{Simulate a multi-species co-occurrence occupancy data set}
\usage{
simulate_occu_multi(
  S = 2,
  N = 300,
  J = 4,
  n_state_covs = 1,
  beta_first = NULL,
  beta_second = NULL,
  beta_p = NULL,
  seed = NULL
)
}
\arguments{
\item{S}{Number of species (default 2).}

\item{N}{Number of sites (default 300).}

\item{J}{Number of replicate visits (default 4).}

\item{n_state_covs}{Number of shared occupancy covariates (default 1).}

\item{beta_first}{Length-\code{S} list of first-order natural-parameter
coefficients \code{c(intercept, slopes...)}. Default moderate occupancy.}

\item{beta_second}{Length-\code{choose(S, 2)} list of second-order (interaction)
coefficients (in \code{combn(S, 2)} order). Default a single positive
interaction for \code{S = 2}, else 0.}

\item{beta_p}{Length-\code{S} list of detection coefficients (logit). Default
\code{c(qlogis(0.5), ...)}.}

\item{seed}{Optional random seed.}
}
\value{
A list with \code{y} (a length-\code{S} list of \verb{N x J} matrices), \code{data},
\code{species}, and \code{truth}.
}
\description{
Draws from the \code{\link[=occu_multi]{occu_multi()}} model: a joint occupancy state for \code{S} species
from the log-linear (first + second order) occupancy model, then per-species
site-level detections at the observed visit pattern.
}

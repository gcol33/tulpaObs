% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmix_laplace_spde.R
\name{nmix_laplace_spde}
\alias{nmix_laplace_spde}
\title{Continuous-field (SPDE) Royle (2004) N-mixture model via nested Laplace}
\usage{
nmix_laplace_spde(
  y,
  site_idx,
  X_lambda,
  X_p,
  spatial,
  mixture = c("P", "NB"),
  r_grid = NULL,
  range_grid = NULL,
  sigma_grid = NULL,
  beta_lambda_init = NULL,
  beta_p_init = NULL,
  u_init = NULL,
  K_max = NULL,
  max_iter = 100L,
  tol = 1e-06,
  verbose = FALSE
)
}
\arguments{
\item{y}{Integer vector of observed counts (long form, one entry per visit).}

\item{site_idx}{Integer vector, 1-based site index for each visit.}

\item{X_lambda}{Numeric matrix \verb{[n_sites x p_lambda]} of abundance covariates.}

\item{X_p}{Numeric matrix \verb{[n_obs x p_p]} of detection covariates.}

\item{spatial}{A \code{tobs_spatial} SPDE term (\code{type == "spde"}) carrying the
FEM \code{tulpa_spec} (\code{A}, \code{C0_diag}, \code{G}, \code{n_mesh}, \code{nu}, \code{prior_range},
\code{prior_sigma}). The projection \code{A} must have \code{n_sites} rows.}

\item{mixture}{Abundance mixing distribution: \code{"P"} (Poisson, default) or
\code{"NB"} (negative binomial). Under \code{"NB"} the NB size \eqn{r} is integrated
as an additional outer grid dimension; the posterior \code{r_mean} / \code{r_sd} are
reported from the grid weights.}

\item{r_grid}{Optional numeric vector of NB size grid points (NB only).
Defaults to \code{exp(seq(log(0.5), log(40), length.out = 6))}.}

\item{range_grid, sigma_grid}{Optional numeric vectors of SPDE range / sigma
grid points. Defaults centre a log-spaced grid on the PC-prior medians.}

\item{beta_lambda_init}{Optional warm start; default \code{c(log(mean(y)+0.1), 0, ...)}.}

\item{beta_p_init}{Optional warm start; default \code{rep(0, p_p)}.}

\item{u_init}{Optional warm start for the mesh field; default zeros.}

\item{K_max}{Truncation for the per-site marginal sum over N. Defaults to
\code{max(y) + 100}.}

\item{max_iter, tol}{Inner Newton iteration budget and gradient-norm tol.}

\item{verbose}{Print per-iteration / per-grid-point progress.}
}
\value{
A list of class \code{nmix_spatial_fit} with the same shape as the areal
fitters plus \code{u_mean} (the posterior-mean mesh field) and
\code{range_mean} / \code{range_sd} / \code{sigma_mean} / \code{sigma_sd}.
}
\description{
Nested-Laplace fit of the spatial N-mixture model with a continuous Matern
(SPDE) field on the abundance arm:
\deqn{N_i \sim \mathrm{Poisson}(\lambda_i), \qquad
      y_{ij} | N_i \sim \mathrm{Binomial}(N_i, p_{ij}),}
\eqn{\log \lambda_i = X_\lambda^{(i)} \beta_\lambda + (A u)_i}, where
\eqn{u \sim \mathrm{N}(0, Q(\mathrm{range}, \sigma)^{-1})} is a Gaussian
Markov random field on the FEM mesh with the proper Matern precision
\eqn{Q}, and \eqn{A} (\eqn{n_{\mathrm{sites}} \times n_{\mathrm{mesh}}})
projects mesh nodes onto sites. The detection arm is
\eqn{\mathrm{logit}\, p_{ij} = X_p^{(ij)} \beta_p}.

The SPDE hyperparameters \eqn{(\mathrm{range}, \sigma)} are integrated out
by the SAME outer grid the areal path (\code{nmix_laplace_icar()} /
\code{nmix_laplace_bym2()} / \code{nmix_laplace_car_proper()}) uses for
\eqn{(\tau[, \rho], \sigma)}: at each grid point the inner Newton finds the
joint mode of \eqn{(\beta_\lambda, \beta_p, u)} and the Laplace log-marginal
\eqn{\log p(y \mid \mathrm{range}_k, \sigma_k)} is accumulated, then the
grid is normalised into posterior weights. The Matern PC priors on
\eqn{(\mathrm{range}, \sigma)} (from the \code{spde()} term) enter each grid
point's log-marginal, mirroring \code{fit_spde()}. The precision \eqn{Q} (and its
\eqn{\log|Q|}) is built once per grid point on the R side via the same FEM
assembly the occupancy SPDE path uses (\code{tulpa:::.spde_precision_Q}), so the
C++ kernel stays agnostic to the precision parameterisation.

Unlike the intrinsic ICAR field, \eqn{Q} is full rank, so the
(intercept, field-mean) direction is identified by \eqn{Q} itself: no
sum-to-zero centering and no constrained-covariance projection (the proper
CAR path is the areal analogue).
}
\references{
Royle, J. A. (2004). N-mixture models for estimating population size from
spatially replicated counts. \emph{Biometrics} 60, 108-115.
Lindgren, F., Rue, H., Lindstrom, J. (2011). An explicit link between
Gaussian fields and Gaussian Markov random fields: the SPDE approach.
\emph{JRSS-B} 73, 423-498.
Fuglstad, G.-A., Simpson, D., Lindgren, F., Rue, H. (2019). Constructing
priors that penalize the complexity of Gaussian random fields. \emph{JASA} 114.
}
\keyword{internal}

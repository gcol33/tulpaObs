% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregation_scan.R
\name{occu_aggregation_scan}
\alias{occu_aggregation_scan}
\title{Suggest spatial and temporal aggregation for an identifiable occupancy model}
\usage{
occu_aggregation_scan(
  data,
  response,
  coords,
  year,
  plot = NULL,
  cell_sizes = NULL,
  block_lengths = NULL,
  score = c("info", "count"),
  family = c("occupancy"),
  control = list()
)
}
\arguments{
\item{data}{Long data.frame, one row per plot-year record (or plot-year-visit).}

\item{response}{Name of the 0/1 detection column.}

\item{coords}{Length-2 character vector naming the x and y coordinate columns.}

\item{year}{Name of the integer year column.}

\item{plot}{Optional name of the plot-id column; enables the within-cell
homogeneity proxy.}

\item{cell_sizes}{Numeric vector of candidate cell edge lengths. \code{NULL}
auto-proposes a geometric ladder from the nearest-neighbour spacing to half
the extent.}

\item{block_lengths}{Integer vector of candidate contiguous block lengths (in
years). \code{NULL} runs mean-shift changepoint segmentation of the per-year
naive occupancy series and uses the resulting blocks as the single temporal
candidate.}

\item{score}{\code{"info"} (curvature-based, default) or \code{"count"} (structural).}

\item{family}{Observation family. Only \code{"occupancy"} is supported; the
per-family constant-model scorer is the extension point for others.}

\item{control}{List of tuning knobs: \code{n_cell_sizes} (auto ladder length, 6),
\code{nn_sample} (NN-spacing sample cap, 1500), \code{min_block} (min years per
segment, 1), \code{cp_penalty} (explicit changepoint penalty, else auto),
\code{cp_penalty_mult} (auto-penalty scale, 1), \code{max_condition} (conditioning
cap for the identifiable flag, 1e6), \code{optim_control} (passed to \code{optim}).}
}
\value{
A \code{tobs_aggregation_scan} object: \code{candidates} (scored data.frame),
\code{recommended} (chosen row or \code{NULL}), \code{segmentation} (auto changepoint
blocks, if used), and scan metadata.
}
\description{
Scans candidate spatial cell sizes and yearly clusterings of long-format,
typically single-visit plot data and scores each by how well a single-season
occupancy model separates occupancy (\code{psi}) from detection (\code{p}). Replication
is manufactured by pooling plots into a spatial cell (assumes plots in a cell
share \code{psi}) and years into a contiguous block (assumes occupancy is closed
across the block); an occasion is any record in a (cell, block) bucket, so
single-visit plots, repeat-visited plots, and the no-pooling limit all flow
through one code path.
}
\details{
Two scoring modes:
\itemize{
\item \code{"info"} fits the constant (intercept-only) occupancy model per candidate
and reports the posterior SEs of \code{psi}/\code{p} and the smallest eigenvalue and
condition number of the 2x2 \verb{(logit psi, logit p)} observed information. A
confounding ridge shows up directly as a near-zero eigenvalue.
\item \code{"count"} checks only the structural necessary conditions (units with two
or more occasions, detected units, a non-degenerate naive detection rate).
}

The recommended candidate is the \strong{least-pooling} identifiable one (smallest
mean occasions per unit, ties broken by largest information eigenvalue): pool
only as much as identifiability requires. When no candidate is identifiable
the data cannot support a standard occupancy model and a Royle-Nichols or
count model is the alternative.
}

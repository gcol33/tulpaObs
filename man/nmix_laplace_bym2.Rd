% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmix_laplace_spatial.R
\name{nmix_laplace_bym2}
\alias{nmix_laplace_bym2}
\title{BYM2 Royle (2004) N-mixture model via nested Laplace}
\usage{
nmix_laplace_bym2(
  y,
  site_idx,
  map_site_to_unit,
  X_lambda,
  X_p,
  adj_row_ptr,
  adj_col_idx,
  n_neighbors,
  n_spatial,
  sigma_grid = NULL,
  rho_grid = NULL,
  mixture = c("P", "NB"),
  r_grid = NULL,
  scale_factor = NULL,
  beta_lambda_init = NULL,
  beta_p_init = NULL,
  v_init = NULL,
  w_init = NULL,
  K_max = NULL,
  max_iter = 100L,
  tol = 1e-06,
  verbose = FALSE
)
}
\arguments{
\item{y}{Integer vector of observed counts (long form, one entry per visit).}

\item{site_idx}{Integer vector, 1-based site index for each visit, same
length as \code{y}.}

\item{map_site_to_unit}{Integer vector of length \code{n_sites}, 1-based spatial
unit index for each site. Sites can share spatial units; the data
contribution to \code{z[u]} aggregates across all sites that map to \code{u}.}

\item{X_lambda}{Numeric matrix \verb{[n_sites x p_lambda]} of abundance covariates.}

\item{X_p}{Numeric matrix \verb{[n_obs x p_p]} of detection covariates.}

\item{adj_row_ptr, adj_col_idx, n_neighbors}{CSR adjacency for the ICAR graph
on the \code{n_spatial} units. \code{adj_row_ptr} has length \code{n_spatial + 1},
\code{adj_col_idx} lists 0-based neighbours, and \code{n_neighbors[s]} is the row
degree of unit s.}

\item{n_spatial}{Number of spatial units.}

\item{sigma_grid}{Numeric vector of \eqn{\sigma} (joint sd) grid points.
Defaults to \code{exp(seq(log(0.2), log(3), length.out = 5L))}.}

\item{rho_grid}{Numeric vector of spatial-fraction grid points in
\eqn{[0, 1]}. Defaults to \code{c(0.05, 0.3, 0.5, 0.7, 0.95)}.}

\item{mixture}{Abundance mixing distribution \code{"P"} (default) or \code{"NB"}; under
\code{"NB"} the NB size \eqn{r} is integrated as an additional outer grid axis.}

\item{r_grid}{Optional NB size grid (NB only); see \code{\link[=nmix_laplace_icar]{nmix_laplace_icar()}}.}

\item{scale_factor}{Optional scalar Riebler scaling factor \eqn{s}, the
geometric mean of the marginal variances \eqn{\mathrm{diag}(Q^{+})} of the
intrinsic ICAR field. If \code{NULL}, it is computed from the adjacency via
dense eigendecomposition.}

\item{beta_lambda_init}{Optional warm start; default \code{c(log(mean(y)+0.1), 0, ...)}.}

\item{beta_p_init}{Optional warm start; default \code{rep(0, p_p)}.}

\item{v_init, w_init}{Optional warm starts (each length \code{n_spatial}) for the
two BYM2 latent components -- the structured (spatial) and unstructured
(iid) effects. \code{NULL} (default) starts both at zero.}

\item{K_max}{Truncation for the per-site marginal sum over N. Defaults to
\code{max(y) + 100}. Returned \code{boundary_max} flags any grid point whose worst
site puts non-trivial mass on \code{K_max} -- raise \code{K_max} if it exceeds 1e-4.}

\item{max_iter, tol}{Inner Newton iteration budget and gradient-norm tol.}

\item{verbose}{Print per-iteration and per-grid-point progress.}
}
\value{
A list of class \code{nmix_spatial_fit}:
\itemize{
\item \code{theta_grid} -- \verb{[n_grid x 2]} matrix of (sigma, rho) per grid point
\item \code{sigma_grid}, \code{rho_grid} -- input axes
\item \code{scale_factor} -- the Riebler scaling factor
\item \code{log_marginal}, \code{weights}, \code{sigma_mean}, \code{sigma_sd}, \code{rho_mean}, \code{rho_sd}
-- posterior summaries of the joint hyperparameters
\item \code{modes} -- \verb{[n_grid x (p_lambda + p_p + 2 n_spatial)]} per-grid modes;
columns \verb{(p_lambda + p_p + 1) .. (p_lambda + p_p + n_spatial)} are \code{v},
the next \code{n_spatial} columns are \code{w}
\item \code{beta_lambda_mean}, \code{beta_p_mean} -- weighted-mean coefficients
\item \code{v_mean}, \code{w_mean} -- weighted-mean ICAR and iid components
\item \code{phi_mean} -- weighted-mean total offset \eqn{\phi}
\item other diagnostic fields as in \code{\link[=nmix_laplace_icar]{nmix_laplace_icar()}}
}
}
\description{
Nested-Laplace fit of the spatial N-mixture model with a BYM2 (Riebler et al.
2016) prior on the abundance-arm spatial offset. The offset decomposes as
\deqn{\phi_u = \sigma \left(\sqrt{\rho / s} \, v_u + \sqrt{1 - \rho} \, w_u\right),}
with \eqn{v \sim \mathrm{ICAR}} (unscaled, sum-to-zero) and
\eqn{w \sim \mathrm{N}(0, I)} iid. \eqn{s} is the Riebler scaling factor,
the geometric mean of the marginal variances \eqn{\mathrm{diag}(Q^{+})} of
the intrinsic ICAR field; \eqn{\sigma} is then the joint marginal standard
deviation of \eqn{\phi}, and \eqn{\rho \in [0, 1]} is the spatial fraction
of variance.

The inner Newton works in the joint state
\eqn{x = (\beta_\lambda, \beta_p, v, w)} (dimension
\eqn{p_\lambda + p_p + 2 n_{\mathrm{spatial}}}). At the converged mode the
Laplace log-marginal is accumulated; the outer 2D grid integrates over
\eqn{(\sigma, \rho)}.
}
\references{
Riebler, A., Sorbye, S. H., Simpson, D., Rue, H. (2016). An intuitive
Bayesian spatial model for disease mapping that accounts for scaling.
\emph{Statistical Methods in Medical Research} 25, 1145-1165.
Morris, M., Wheeler-Martin, K., Simpson, D., Mooney, S. J., Gelman, A.,
DiMaggio, C. (2019). Bayesian hierarchical spatial models: Implementing
the BYM2 model in Stan. \emph{Spatial and Spatio-temporal Epidemiology} 31.
}

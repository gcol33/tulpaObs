% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{simulate_ms_count}
\alias{simulate_ms_count}
\title{Simulate community relative-abundance (count / continuous) data}
\usage{
simulate_ms_count(
  N = 120,
  n_species = 10,
  beta_comm_mean = c(1, 0.5),
  beta_comm_sd = c(0.4, 0.3),
  response = c("poisson", "negbin", "gaussian"),
  size = 2,
  size.log.sd = 0.3,
  sd = 1,
  seed = NULL
)
}
\arguments{
\item{N}{Number of sites.}

\item{n_species}{Number of species.}

\item{beta_comm_mean, beta_comm_sd}{Community mean and SD of the per-species
coefficients (intercept first). Length sets the number of covariates.}

\item{response}{One of \code{"poisson"}, \code{"negbin"}, \code{"gaussian"}.}

\item{size}{Negative-binomial community size (mean of the per-species
\code{log_r}); \code{size.log.sd} is its across-species SD.}

\item{size.log.sd}{Across-species SD of \code{log(size)} (negbin only).}

\item{sd}{Gaussian residual SD.}

\item{seed}{Optional RNG seed.}
}
\value{
A list with \code{y} (an \verb{N x n_species} matrix), \code{data}, and \code{truth}.
}
\description{
Per-species GLMM with Gaussian community hyperpriors on the coefficients (the
\code{ms_count()} / spAbundance \code{msAbund} model): \verb{g(mu_\{s,i\}) = X_i beta_s},
\code{beta_s ~ N(beta_comm_mean, diag(beta_comm_sd^2))}, \verb{y_\{s,i\}} drawn Poisson /
negative-binomial (log link) or Gaussian (identity). No detection.
}

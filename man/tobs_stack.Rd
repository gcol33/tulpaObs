% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stacking.R
\name{tobs_stack}
\alias{tobs_stack}
\alias{print.tobs_stack}
\alias{fitted.tobs_stack}
\alias{predict.tobs_stack}
\title{Stack fitted models into a LOO-weighted ensemble}
\usage{
tobs_stack(..., method = c("stacking", "pseudobma"))

\method{print}{tobs_stack}(x, ...)

\method{fitted}{tobs_stack}(object, ...)

\method{predict}{tobs_stack}(
  object,
  X.0 = NULL,
  quantiles = c(0.025, 0.5, 0.975),
  n.draws = 4000L,
  ...
)
}
\arguments{
\item{...}{ignored.}

\item{method}{weighting scheme passed to \code{\link[loo:loo_model_weights]{loo::loo_model_weights()}}:
\code{"stacking"} (default) or \code{"pseudobma"}.}

\item{x}{a \code{tobs_stack} object.}

\item{object}{a \code{tobs_stack} object.}

\item{X.0}{optional occupancy design matrix for out-of-sample prediction.
When supplied, all members must share the same occupancy design.}

\item{quantiles}{credible-interval quantile levels.}

\item{n.draws}{size of the pooled stacked-predictive sample (design-matrix
mode); each member contributes a share proportional to its weight.}
}
\value{
An object of class \code{tobs_stack}: a list with \code{weights} (named
numeric, summing to 1), \code{fits} (the members), \code{loo} (per-member
\code{\link[loo:loo]{loo::loo()}} objects), \code{comparison} (a data frame of \code{elpd_loo}, \code{weight},
and worst Pareto-k per member), and \code{method}. \code{\link[=predict]{predict()}} / \code{\link[=fitted]{fitted()}} on
the result return the weight-combined predictive.
}
\description{
Combines two or more fitted models into one predictive distribution with
leave-one-out predictive stacking. Each member contributes its
Pareto-smoothed importance-sampling LOO predictive density; the weights
maximize the stacked leave-one-out predictive density and so favour members
that predict held-out observations best. Works across every family
(single-season / community / dynamic / integrated occupancy, JSDM, and the
cover hurdle), each scored by the marginal likelihood in
\code{.tobs_pointwise_loglik()}.
}
\details{
The members must be fit to the same observation set. They may otherwise
differ freely -- different covariates, priors, or inference \code{method}s. (An
ensemble of pure seed-variants, e.g. from \code{tobs(..., control = list(n.seeds = K))}, is a valid but degenerate case: the members are statistically
identical, so the weights come out roughly uniform.)
}
\examples{
\dontrun{
f1 <- tobs(~ elev,          data = sites, family = occu(),
           detection = ~ effort, y = y, method = "nuts")
f2 <- tobs(~ elev + forest, data = sites, family = occu(),
           detection = ~ effort, y = y, method = "nuts")
ens <- tobs_stack(simple = f1, full = f2)
ens$weights
predict(ens)            # weight-combined in-sample psi / p / z
}
}

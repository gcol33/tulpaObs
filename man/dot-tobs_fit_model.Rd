% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occu_fit.R
\name{.tobs_fit_model}
\alias{.tobs_fit_model}
\title{Internal engine entry point}
\usage{
.tobs_fit_model(
  model,
  method = c("laplace", "nested_laplace", "nuts"),
  priors = NULL,
  sigma.beta = 10,
  sigma.re.scale = 1,
  max.iter = 100L,
  tol = 1e-04,
  damping = 0.7,
  n.iter = 2000,
  n.warmup = 1000,
  n.thin = 1L,
  n.chains = 1L,
  n.threads = 1L,
  max.treedepth = 10,
  adapt.delta = 0.8,
  seed = 42,
  approx = c("gaussian_laplace", "simplified_laplace"),
  correction = "none",
  n.gibbs = 10L,
  n.imputations = 20L,
  K.max = NULL,
  mixture = "poisson",
  verbose = TRUE,
  ...
)
}
\description{
Dispatches to Laplace (default) or NUTS for a built \code{tobs_model}. Not
user-facing; called from \code{tobs()} via the per-family \verb{.dispatch_*} helpers.
Spatial / temporal / random-effect / SVC / latent structure is read from
the structured terms the formula carried (\code{model$structured_terms}), not
from arguments — there is a single user-facing specification path.
}
\keyword{internal}

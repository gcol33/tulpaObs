% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occu_fit.R
\name{occu_fit}
\alias{occu_fit}
\title{Fit an occupancy model}
\usage{
occu_fit(
  model,
  spatial = NULL,
  temporal = NULL,
  re = NULL,
  svc = NULL,
  latent = NULL,
  method = c("laplace", "nuts"),
  sigma_beta = 10,
  sigma_re_scale = 1,
  max_iter = 100L,
  tol = 1e-04,
  damping = 0.7,
  iter = 2000,
  warmup = 1000,
  max_treedepth = 10,
  adapt_delta = 0.8,
  seed = 42,
  verbose = TRUE
)
}
\arguments{
\item{model}{A \code{tulpaObs} object from \code{\link[=occu]{occu()}}.}

\item{spatial}{Optional spatial specification from \code{\link[=occu_icar]{occu_icar()}}, \code{\link[=occu_bym2]{occu_bym2()}},
\code{\link[=occu_gp]{occu_gp()}}, \code{\link[=occu_multiscale_gp]{occu_multiscale_gp()}}, or \code{\link[=occu_spde]{occu_spde()}}.}

\item{temporal}{Optional temporal specification from \code{\link[=occu_temporal]{occu_temporal()}}.}

\item{re}{Optional list of random effect specifications from \code{\link[=occu_re]{occu_re()}}.
A single \code{occu_re} object is also accepted.}

\item{svc}{Optional SVC specification from \code{\link[=occu_svc]{occu_svc()}}.}

\item{latent}{Optional latent factor specification from \code{\link[=occu_latent]{occu_latent()}}.}

\item{method}{Inference method: \code{"laplace"} (default, fast) or \code{"nuts"} (exact MCMC).}

\item{sigma_beta}{Prior SD for regression coefficients (default 10).}

\item{sigma_re_scale}{Prior scale for RE standard deviations (default 1).
Only used for community models with species RE.}

\item{max_iter}{Maximum EM iterations for Laplace (default 50).}

\item{tol}{Convergence tolerance for Laplace EM (default 1e-4).}

\item{damping}{EM damping factor 0-1 for Laplace (default 0.3).}

\item{iter}{Total NUTS iterations (default 2000). Only used if \code{method = "nuts"}.}

\item{warmup}{Warmup iterations (default 1000). Only used if \code{method = "nuts"}.}

\item{max_treedepth}{Maximum NUTS tree depth (default 10).}

\item{adapt_delta}{Target acceptance rate (default 0.8).}

\item{seed}{Random seed (default 42).}

\item{verbose}{Print progress (default TRUE).}
}
\value{
A \code{tulpaObs_fit} object with posterior draws and diagnostics.
}
\description{
Unified fitting function for all tulpaObs model types.
Default method is Laplace approximation (fast, Tier 2).
Use \code{method = "nuts"} for full MCMC posterior sampling (Tier 1).
}

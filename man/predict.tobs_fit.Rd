% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{predict.tobs_fit}
\alias{predict.tobs_fit}
\title{Predict from occupancy model}
\usage{
\method{predict}{tobs_fit}(
  object,
  X.0 = NULL,
  type = c("occupancy", "detection", "both", "state"),
  quantiles = c(0.025, 0.5, 0.975),
  terms = NULL,
  n_points = 50L,
  newdata = NULL,
  times = NULL,
  level = 0.95,
  nsim = 1000L,
  draws = TRUE,
  time_col = NULL,
  X_det.0 = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{tobs_fit} object.}

\item{X.0}{Optional design matrix for occupancy prediction.}

\item{type}{\code{"occupancy"} (default), \code{"detection"}, \code{"both"}, or \code{"state"}
(nested-Laplace marginalised per-site psi, incl. held-out sites). For an
\code{occu_cover} fit: \code{"occurrence"}, \code{"cover_cond"}, \code{"cover_exp"}, or
\code{"change"}. \code{"detection"} / \code{"both"} on a \code{single}/\code{int_occu()} fit needs
\code{X_det.0}.}

\item{quantiles}{Quantile levels for credible intervals: a strictly
increasing length-3 vector (lower, middle, upper), each in (0, 1). The
levels name the columns they fill, so \code{c(0.05, 0.5, 0.95)} reports
\code{q5} / \code{q50} / \code{q95}, and the levels used travel on the returned table in
\code{attr(, "quantiles")}.}

\item{terms}{Name of a single term to vary: one column of the predicted
process's design matrix, every other column held at its column mean. A
vector of more than one term is an error, since a second term would be
held at its mean rather than grouped over its levels. For a grid over
more than one covariate, build the design matrix and pass it as \code{X.0}.}

\item{n_points}{Number of prediction points per continuous term.}

\item{newdata}{\code{occu_cover} only: data.frame of prediction units, one row per
field cell (or carrying a \code{cell} column mapping rows to field cells).
Defaults to the training data.}

\item{times}{\code{occu_cover} \code{type = "change"} only: length-2 numeric
\code{c(t1, t2)}, the two values of the time covariate to difference.}

\item{level}{\code{occu_cover} only: credible level for the interval columns
(default 0.95).}

\item{nsim}{\code{occu_cover} only: number of joint posterior draws (default 1000).}

\item{draws}{\code{occu_cover} only: if \code{TRUE} (default), carry the per-unit
\verb{[cell x nsim]} draw matrices in \code{attr(, "draws")}.}

\item{time_col}{\code{occu_cover} only: name of the time covariate weighting the
trend field / driving the change map; auto-resolved from the fit's stored
trend weight when omitted.}

\item{X_det.0}{Optional detection design for out-of-sample \code{"detection"} /
\code{"both"} prediction on a \code{single}-season or \code{\link[=int_occu]{int_occu()}} fit: a plain
design matrix for a single-season fit, or
a list of one design matrix per source (named by source, or in source
order) for an \code{int_occu()} fit -- matching \code{fitted()$p}'s per-source
shape. In-sample locations only: like the state-arm \code{svc()}/\code{spde()}
surfaces, this does not krige a spatial detection field to unseen points.}

\item{...}{Ignored.}
}
\value{
Depends on mode. In-sample: \code{fitted()} result. \code{"state"}: a
data.frame with \code{row}, \code{psi} (marginalised posterior mean), \code{psi_lower} /
\code{psi_upper} (equal-tailed 95\% credible interval; \code{NA} when the engine did
not return per-cell predictive variance), and \code{heldout}. Design-matrix/
terms: data.frame with estimate and CIs; \code{type = "detection"} on an
\code{int_occu()} fit returns a named list of such data.frames (one per
source), and \code{type = "both"} returns \code{list(occupancy = , detection = )}.
\code{occu_cover}: a \code{tobs_prediction} table (one row per cell) with per-unit
draw matrices in \code{attr(, "draws")}.
}
\description{
Four modes:
\itemize{
\item \strong{In-sample}: \code{predict(fit)} returns fitted values.
\item \strong{State posterior / NA-response}: \code{predict(fit, type = "state")} returns
the marginalised per-site occupancy posterior of a \code{"nested_laplace"} fit:
the per-row eta posterior is a Gaussian mixture over the hyperparameter
grid (per-cell fitted linear predictor and predictive variance), so \code{psi}
is its Gauss-Hermite mean and \code{psi_lower} / \code{psi_upper} are the mixture-CDF
quantiles -- a calibrated 95\% credible interval, exact for every latent
prior. Rows with \code{heldout = TRUE} are the INLA-style NA-response
prediction targets (single-season sites whose detection history was
all-missing), where occupancy is interpolated from the latent field rather
than informed by detections.
\item \strong{Design-matrix}: \code{predict(fit, X.0 = ...)} predicts at new covariate values.
\item \strong{Terms-based}: \code{predict(fit, terms = "elev")} varies one covariate, others at mean.
\item \strong{Joint occu_cover}: for a \code{occu_cover} joint-coupled fit,
\code{predict(fit, newdata, type = "occurrence" | "cover_cond" | "cover_exp" | "change")} samples the joint latent from the grid-integrated posterior
(the outer-grid mixture via \code{\link[tulpa:tulpa_posterior_draws]{tulpa::tulpa_posterior_draws()}}) and
marginalises every derived quantity per draw. \code{type = "change"} with
\code{times = c(t1, t2)} returns a per-cell change table (\code{delta_p},
\code{delta_cover_cond}, \code{delta_cover_exp}, the occupancy / abundance
decomposition, and \code{.lwr} / \code{.upr} at \code{level}), plus the start / end
occupancy \code{p_T1} / \code{p_T2} with their own \code{.sd} / \code{.lwr} / \code{.upr}, and a
\code{.prob_pos} column per headline delta giving the directional posterior
probability \code{P(delta > 0)} per cell. The result is a
\code{tobs_prediction} table (one row per cell) carrying per-unit \verb{[cell x nsim]}
draw matrices in \code{attr(, "draws")}; map it yourself, e.g.
\code{left_join(cents, pr, by = "cell")} then
\code{geom_tile(aes(x, y, fill = delta_p))} (or \code{geom_sf()} on polygon cells).
\item \strong{Spatial-factor community}: for a reduced-rank spatial-factor
\code{ms_occu_cover()} fit, \code{predict(fit, type = "occupancy" | "cover_cond" | "cover_exp")} returns the per-species per-cell posterior as a long table
(one row per cell x species): \code{"occupancy"} gives \code{psi}, \code{"cover_cond"} the
conditional cover mean \code{E[cover | present]}, \code{"cover_exp"} the unconditional
expected cover \code{psi * E[cover | present]}, each with a \verb{_median} and a
\verb{_lower} / \verb{_upper} interval. The maps are marginalised over the loading +
field posterior, so a rare species borrows strength across the shared
factors for a calibrated map. The latent fields are tied to the cell graph,
so \code{X.0} / \code{newdata} prediction is not supported.
}
}

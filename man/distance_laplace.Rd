% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance.R
\name{distance_laplace}
\alias{distance_laplace}
\title{Laplace fit of the binned distance-sampling abundance model}
\usage{
distance_laplace(
  y,
  X_lambda,
  X_sigma,
  cutpoints,
  key = c("halfnorm", "hazard"),
  transect = c("line", "point"),
  mixture = c("P", "NB"),
  beta_lambda_init = NULL,
  beta_sigma_init = NULL,
  eta_b_init = NULL,
  log_r_init = NULL,
  r_max = 1e+05,
  K_max = NULL,
  headroom = NULL,
  quad_order = 64L,
  max_iter = 100L,
  tol = 1e-06,
  verbose = FALSE,
  quad_xptr = NULL
)
}
\arguments{
\item{y}{Integer matrix \verb{[n_sites x n_bins]} of per-bin detected counts.}

\item{X_lambda}{Numeric matrix \verb{[n_sites x p_lambda]} of abundance covariates.}

\item{X_sigma}{Numeric matrix \verb{[n_sites x p_sigma]} of detection-scale
covariates (the \verb{log sigma} linear predictor).}

\item{cutpoints}{Numeric bin edges, length \code{n_bins + 1}
(\verb{0 = c_0 < c_1 < ... < c_B}).}

\item{key}{\code{"halfnorm"} (default) or \code{"hazard"} detection key.}

\item{transect}{\code{"line"} (default, distances uniform) or \code{"point"} (radial,
density proportional to distance).}

\item{mixture}{\code{"P"} (Poisson, default) or \code{"NB"} (negative binomial).}

\item{beta_lambda_init, beta_sigma_init, eta_b_init, log_r_init}{Optional warm
starts.}

\item{r_max}{Upper bound on the NB size \code{r} (NB only, default \code{1e5}).}

\item{K_max}{Marginal-sum truncation; defaults to \verb{3 * max(site total) + 100}
(applied per site, see \code{headroom}).}

\item{headroom}{Latent-N states summed above each site's own detected total.
\code{NULL} (default) derives it from \code{K_max}: an unset \code{K_max} caps each site
at its own total plus \verb{2 * max(site total) + 100}, an explicit \code{K_max}
truncates globally and uncapped. A negative value disables the per-site
cap.}

\item{quad_order}{Gauss-Legendre nodes per bin (default 64).}

\item{max_iter}{Newton iteration budget (default 100).}

\item{tol}{Gradient-norm convergence tolerance (default 1e-6).}

\item{verbose}{Print per-iteration progress.}

\item{quad_xptr}{Optional pre-built quadrature (from \code{cpp_distance_build_quad()})
for a caller fitting several species/starts against the same \code{cutpoints}
/ \code{transect} / \code{quad_order}; built once and reused instead of every call
rebuilding it. Internal; defaults to building it fresh.}
}
\value{
A list of class \code{distance_fit} with \code{beta_lambda}, \code{beta_sigma},
\code{shape} / \code{eta_b} (hazard-rate), \code{log_r} / \code{r} (NB), \code{log_lik}, \code{vcov},
\code{H_obs}, per-site \code{mean_N} / \code{var_N} / \code{p_det} / \code{boundary_weight}.
}
\description{
Maximum-likelihood fit (non-spatial, fixed effects only) of a binned
distance-sampling abundance model with a Poisson or negative-binomial
abundance mixing distribution and a half-normal or hazard-rate detection key.
Latent abundance \code{N_i ~ Poisson(lambda_i)} (or \code{NegBin(lambda_i, r)}) is summed
out in closed form (truncation \code{K_max}); the detection integrals over distance
bins are evaluated by Gauss-Legendre quadrature.
}
\references{
Buckland, S. T., et al. (2001). Introduction to Distance Sampling. Oxford.
Royle, J. A., Dawson, D. K., Bates, S. (2004). Modeling abundance effects in
distance sampling. \emph{Ecology} 85, 1591-1597.
}

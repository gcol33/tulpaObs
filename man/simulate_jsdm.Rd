% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{simulate_jsdm}
\alias{simulate_jsdm}
\title{Simulate joint species distribution (presence/absence) data}
\usage{
simulate_jsdm(
  N = 100,
  n_species = 10,
  beta_comm_mean = c(0, 0.5),
  beta_comm_sd = c(0.5, 0.3),
  seed = NULL
)
}
\arguments{
\item{N}{Number of sites (default 100).}

\item{n_species}{Number of species (default 10).}

\item{beta_comm_mean}{Community-mean occupancy coefficients, length
\code{1 + n_occ_covs} (intercept first).}

\item{beta_comm_sd}{Between-species SD of each occupancy coefficient (same
length as \code{beta_comm_mean}).}

\item{seed}{Random seed.}
}
\value{
A list with \code{y} (an \verb{N x n_species} 0/1 presence matrix), \code{data}, and
\code{truth} (per-species coefficients and the community hyperparameters).
}
\description{
Presence \verb{y_\{i,s\} ~ Bernoulli(psi_\{i,s\})} with \verb{logit psi = X beta_s}, the
per-species occupancy coefficients drawn from Gaussian community hyperpriors.
Matches the \code{\link[=jsdm]{jsdm()}} family, which observes presence directly (no detection
process), so the response is an \verb{N x n_species} presence matrix.
}
\examples{
sim <- simulate_jsdm(N = 60, n_species = 5, seed = 1)
dim(sim$y)   # 60 sites x 5 species presence matrix
}

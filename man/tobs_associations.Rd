% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ms_occu_cover_spatial.R
\name{tobs_associations}
\alias{tobs_associations}
\title{Residual species-association matrices from a spatial-factor community fit}
\usage{
tobs_associations(
  object,
  type = c("occupancy", "cover", "cross"),
  summary = NULL
)
}
\arguments{
\item{object}{A fitted \code{tobs_fit} from a spatial-factor \code{ms_occu_cover()}.}

\item{type}{Which association to return: \code{"occupancy"} (default), \code{"cover"},
or \code{"cross"} (occupancy vs cover). \code{"cover"} and \code{"cross"} require a
cover-arm factor (an \code{icar()}/\code{car_proper()}/\code{bym2()} term on the cover
formula).}

\item{summary}{One of \code{"median"}, \code{"estimate"} (rotation-invariant point
estimate at the posterior mode), \code{"lower"}, \code{"upper"}; or \code{NULL} (default)
to return the full list of all four \verb{S x S} matrices plus the interval
\code{prob} and draw count.}
}
\value{
An \verb{S x S} correlation matrix (when \code{summary} is given) or a list of
the point estimate, posterior median, and interval bounds.
}
\description{
For a reduced-rank spatial-factor community occupancy-cover fit (a
\code{\link[=ms_occu_cover]{ms_occu_cover()}} model carrying an \code{icar()}, \code{car_proper()}, or \code{bym2()}
field on the occupancy arm), return the residual species associations the
shared latent fields imply -- the spatial-JSDM / HMSC output. The K unit-scale
fields induce a residual occupancy covariance \verb{L L'} across species; the
reported matrix is its correlation form, identified (invariant to the factor
rotation, sign, and column scale). When the fit also carries a cover-arm
factor, the cover association \verb{corr(L_pos L_pos')} and the joint cross-arm
association (standardized \verb{L_occ L_pos'}, species occupancy vs species cover)
are available too.
}
\details{
Each matrix is summarised from draws of the loading posterior (not the plug-in
mode), so a central interval accompanies the point estimate.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{occu_spde}
\alias{occu_spde}
\title{SPDE Spatial Random Effect (Matérn via Triangular Mesh)}
\usage{
occu_spde(
  coords,
  data = NULL,
  mesh = NULL,
  max_edge = NULL,
  cutoff = 0,
  nu = 1,
  shared = c(TRUE, FALSE),
  prior_range = c(0.5, 0.5),
  prior_sigma = c(1, 0.5)
)
}
\arguments{
\item{coords}{Matrix of coordinates (n_sites x 2) or a formula \code{~ x + y}.}

\item{data}{Optional data.frame for formula evaluation.}

\item{mesh}{A pre-built \code{tulpa_mesh} object (from tulpaMesh). If NULL,
built automatically from coords.}

\item{max_edge}{Maximum mesh edge length. Scalar or \code{c(inner, outer)}.}

\item{cutoff}{Minimum distance between mesh vertices. Default 0.}

\item{nu}{Matérn smoothness parameter. Default 1. Fractional values (0.5, 1.5)
use rational SPDE approximation.}

\item{shared}{Which processes get the spatial effect.
Default \code{c(TRUE, FALSE)} = occupancy only.}

\item{prior_range}{Prior for range: \code{c(U, alpha)} where P(range < U) = alpha.}

\item{prior_sigma}{Prior for sigma: \code{c(U, alpha)} where P(sigma > U) = alpha.}
}
\value{
A \code{tulpaObs_spatial} object
}
\description{
Specifies a continuous Matérn spatial field for occupancy models using the
SPDE approach. Builds a triangular mesh and FEM matrices via tulpaMesh,
then uses tulpa's CHOLMOD-accelerated SPDE Laplace engine.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family_cover_hurdle.R
\name{decode_cover_hurdle}
\alias{decode_cover_hurdle}
\title{Decode the two-arm fit into a cover_fit object}
\usage{
decode_cover_hurdle(fits, enc, family, approx = "gaussian_laplace")
}
\description{
Extracts beta vectors and SEs for each arm. SEs are scaled to match
each arm's dispersion convention:
}
\details{
\itemize{
\item lognormal arm: \code{tulpa_laplace(family = "gaussian")} computes the
Hessian assuming phi = 1, so SEs are rescaled by \code{sigma_pos^2}.
\item beta arm: \code{tulpa_laplace_beta()} already weights the Hessian by phi
(Fisher information), so SEs are returned at scale 1.
}

When \code{approx = "simplified_laplace"}, the cover-hurdle SLA gamma is
computed via \code{\link[=.sla_compute_cover_hurdle]{.sla_compute_cover_hurdle()}}: a per-arm 5-point FD of
the \emph{original} Bernoulli / Beta / Lognormal log-likelihood against the
arm's \code{solve(H_beta)} Sigma (raw Hessian — no Louis correction needed
here because both arms run real likelihoods at the mode, not the
pseudo-binomial M-step encoding). Per-arm pseudo-draws are then
resampled from skew-normals fit by moment-matching \verb{(beta_arm, se_arm, gamma_arm)}.
}
\keyword{internal}

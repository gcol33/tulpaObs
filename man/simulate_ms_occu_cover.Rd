% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ms_occu_cover.R
\name{simulate_ms_occu_cover}
\alias{simulate_ms_occu_cover}
\title{Simulate community (multispecies) joint occupancy-cover data}
\usage{
simulate_ms_occu_cover(
  n_species = 12,
  N = 120,
  J = 5,
  n_occ_covs = 1,
  n_det_covs = 1,
  n_pos_covs = 1,
  mu_occ = NULL,
  mu_p = NULL,
  mu_pos = NULL,
  sd_occ = 0.5,
  sd_p = 0.4,
  sd_pos = 0.4,
  positive = c("lognormal", "beta"),
  phi = 30,
  sigma_pos = 0.4,
  seed = NULL
)
}
\arguments{
\item{n_species}{Number of species (default 12).}

\item{N}{Number of sites / cells (default 120).}

\item{J}{Number of replicate visits (default 5).}

\item{n_occ_covs, n_det_covs, n_pos_covs}{Number of covariates on each arm
(drawn IID standard normal, shared across species).}

\item{mu_occ, mu_p, mu_pos}{Community-mean coefficient vectors
\code{c(intercept, slopes...)} on each arm's link scale. Defaults pick
weakly-informative values.}

\item{sd_occ, sd_p, sd_pos}{Per-coefficient community SD on each arm (length 1,
recycled, or one per coefficient). Default 0.5 / 0.4 / 0.4.}

\item{phi}{Beta precision when \code{positive = "beta"} (default 30).}

\item{sigma_pos}{Lognormal residual SD when \code{positive = "lognormal"}
(default 0.4).}

\item{seed}{Optional integer seed.}

\item{response}{\code{"lognormal"} (default) or \code{"beta"}.}
}
\value{
A list with \code{y} (3D detection array), \code{y_pos} (3D cover array, \code{NA}
where not detected), \code{data} (per-cell covariate frame), \code{visit_data}
(per-visit covariate frame, \code{N*J} rows in site-major order), \code{species}
(species names), and \code{truth} (community means / SDs, per-species
coefficients, the dispersion, and the latent state).
}
\description{
Per-species joint occupancy-detection + cover model with Gaussian community
hyperpriors on the per-species coefficients of all three arms:
\code{beta_occ_s ~ N(mu_occ, diag(sd_occ^2))},
\code{beta_p_s ~ N(mu_p, diag(sd_p^2))},
\code{beta_pos_s ~ N(mu_pos, diag(sd_pos^2))}, then per cell
\verb{z_\{s,i\} ~ Bernoulli(psi_\{s,i\})}, per visit
\verb{y_\{s,i,j\} | z = 1 ~ Bernoulli(p_\{s,i,j\})}, and per detected visit
\verb{c_\{s,i,j\} ~ f_pos(eta_pos_\{s,i,j\}, disp)}. The site and visit covariates are
shared across species (community covariates). The returned \code{y} / \code{y_pos} are
3D arrays \verb{[n_sites x J x n_species]} suitable for \code{\link[=tobs]{tobs()}} with
\code{\link[=ms_occu_cover]{ms_occu_cover()}} (\code{y_pos} is \code{NA} where not detected).
}

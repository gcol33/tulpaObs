% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ms_abun.R
\name{simulate_ms_abun}
\alias{simulate_ms_abun}
\title{Simulate community (multispecies) N-mixture abundance data}
\usage{
simulate_ms_abun(
  n_species = 12,
  N = 80,
  J = 4,
  n_abund_covs = 1,
  n_det_covs = 1,
  mu_lambda = NULL,
  mu_p = NULL,
  sd_lambda = 0.5,
  sd_p = 0.4,
  mixture = c("poisson", "negbin", "zip", "zinb"),
  size = 3,
  sigma_logr = 0.4,
  omega = 0.3,
  sigma_omega = 0.4,
  graph = NULL,
  sigma.field = 0.6,
  seed = NULL
)
}
\arguments{
\item{n_species}{Number of species (default 12).}

\item{N}{Number of sites (default 80).}

\item{J}{Number of replicate visits (default 4).}

\item{n_abund_covs}{Number of abundance covariates (default 1).}

\item{n_det_covs}{Number of detection covariates (default 1).}

\item{mu_lambda}{Community-mean abundance coefficients \code{c(intercept, slopes...)} on the log scale. Default \code{c(log(3), rep(0.4, n_abund_covs))}.}

\item{mu_p}{Community-mean detection coefficients on the logit scale.
Default \code{c(0.3, rep(-0.3, n_det_covs))}.}

\item{sd_lambda}{Per-coefficient community SD for the abundance arm (the
sqrt-diagonal of \code{Sigma_lambda}). Length 1 (recycled) or \code{1 + n_abund_covs}.
Default 0.5.}

\item{sd_p}{Per-coefficient community SD for the detection arm. Default 0.4.}

\item{mixture}{Abundance mixing distribution: \code{"poisson"} (default),
\code{"negbin"}, or their zero-inflated counterparts \code{"zip"} / \code{"zinb"} (a
per-species structural-zero share).}

\item{size}{Community-mean negative-binomial size, equal to
\eqn{\exp(\mu_{\log r})} (ignored under Poisson). Default 3. The per-species
sizes are \eqn{r_s = \exp(\mu_{\log r} + b^{\log r}_s)} with
\eqn{b^{\log r}_s \sim N(0, \sigma_{\log r}^2)}.}

\item{sigma_logr}{Standard deviation of the per-species log-dispersion random
effect \code{log_r_s} (used only under \code{mixture = "negbin"} / \code{"zinb"}). Default
0.4. Set to 0 for a shared (single-\code{r}) community.}

\item{omega}{Community-mean structural-zero probability, equal to
\eqn{\mathrm{plogis}(\mu_\omega)} (used only under \code{mixture = "zip"} /
\code{"zinb"}). Default 0.3. The per-species structural-zero probabilities are
\eqn{\omega_s = \mathrm{plogis}(\mu_\omega + b^\omega_s)} with
\eqn{b^\omega_s \sim N(0, \sigma_\omega^2)}.}

\item{sigma_omega}{Standard deviation of the per-species structural-zero-logit
random effect \code{logit_omega_s} (used only under zero-inflation). Default 0.4.
Set to 0 for a shared (single-\code{omega}) community.}

\item{graph}{Optional \verb{N x N} 0/1 adjacency matrix. When supplied, a single
shared spatial field \code{f} (one value per site) is drawn from a proper GMRF on
the graph, centered and scaled to standard deviation \code{sigma.field}, and
added to every species' abundance log-linear predictor
(\verb{log lambda_\{s,i\} = X_lambda_i . beta_lambda_s + f_i}). \code{N} is taken from
\code{nrow(graph)}. The truth field is returned in \code{truth$field}.}

\item{sigma.field}{Standard deviation of the shared spatial field (used only
when \code{graph} is supplied). Default 0.6.}

\item{seed}{Optional random seed.}
}
\value{
A list with \code{y} (3D count array), \code{data} (site covariate frame),
\code{species} (species names), and \code{truth} (community means / SDs, the
per-species coefficients, lambda, p, latent N, the shared \code{field} when
\code{graph} is given, and -- under \code{negbin} -- \code{mu_log_r}, \code{sigma_log_r}, the
per-species \code{b_logr} / \code{r_s}).

\code{truth} carries each community mean twice. \code{mu_lambda}, \code{mu_p},
\code{mu_log_r} and \code{mu_omega} are the POPULATION constants the per-species
values were drawn around. \code{mu_lambda_real}, \code{mu_p_real}, \code{mu_log_r_real}
and \code{mu_omega_real} are the mean of the values this seed actually drew,
which sits \code{sd / sqrt(n_species)} from the constant -- 0.12 on
\code{mu_log_r} at \code{sigma_logr = 0.5} and 18 species.

Which one to score against follows from the question. An interval for a
community mean targets the POPULATION constant and its width includes that
\code{sd^2 / n_species} term, so a coverage measurement scores against
\code{mu_log_r}. A point-recovery or interval-SCALE measurement scores against
\code{mu_log_r_real}: against the constant, the draw carries about two thirds
of the spread across seeds, and a seed block that happens to draw wide
reads as a miscalibrated interval. That is what
\code{gcol33/tulpaObs#280} and \verb{#285} measured -- the 18-species block behind
both drew its species means 18-21\% wider than \code{sigma_logr / sqrt(18)},
and both arms calibrate once the draw is put at its expectation. The
\verb{_real} entries are supplied so the split does not have to be rebuilt at
each call site; \code{tests/testthat/helper-community-mean.R} is the assertion
that consumes them.
}
\description{
Per-species Royle (2004) N-mixture with Gaussian community hyperpriors:
\code{beta_lambda_s ~ N(mu_lambda, Sigma_lambda)},
\code{beta_p_s ~ N(mu_p, Sigma_p)}, then \verb{N_\{s,i\} ~ Poisson(lambda_\{s,i\})}
(or \code{NegBin(mu = lambda, size = r_s)} with a per-species size
\code{r_s = exp(mu_log_r + b_logr_s)}) and
\verb{y_\{s,i,j\} ~ Binomial(N_\{s,i\}, p_\{s,i,j\})}. The returned \code{y} is a 3D array
\verb{[n_sites x J x n_species]} suitable for \code{\link[=tobs]{tobs()}} with \code{\link[=ms_abun]{ms_abun()}}.
}

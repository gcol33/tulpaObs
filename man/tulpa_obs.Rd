% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tulpa_obs.R
\name{tulpa_obs}
\alias{tulpa_obs}
\title{Fit a hierarchical latent-state observation model}
\usage{
tulpa_obs(
  formula,
  data,
  family,
  detection = NULL,
  y = NULL,
  visit_data = NULL,
  spatial = NULL,
  temporal = NULL,
  engine = c("auto", "laplace", "nested_laplace", "nuts"),
  priors = NULL,
  control = list(),
  ...
)
}
\arguments{
\item{formula}{state-process formula, e.g. \code{~ elev + forest}. For
occupancy this is the occupancy probability formula; for N-mixture
the abundance formula; for cover hurdle the latent-presence formula.}

\item{data}{data frame of site-level covariates with \code{nrow(data) == nrow(y)}.}

\item{family}{a \code{tulpa_obs_family} object (see \code{\link[=obs_family]{obs_family()}} and the
concrete constructors \code{\link[=occ]{occ()}}, \code{\link[=nmixture]{nmixture()}}, \code{\link[=cover_hurdle]{cover_hurdle()}}, ...).}

\item{detection}{detection-process formula, e.g. \code{~ observer + effort}.
Family-dependent: required for \code{occ()} and \code{nmixture()}, ignored for
\code{jsdm()} and (currently) \code{cover_hurdle()}.}

\item{y}{response. Shape depends on family:
\itemize{
\item \code{occ()} — N x J detection-history matrix.
\item \code{nmixture()} — N x J integer count matrix.
\item \verb{multispecies_*} — S x N x J array.
\item \code{cover_hurdle()} — length-N vector of cover proportions in [0, 1].
}}

\item{visit_data}{optional data frame of visit-level detection
covariates with \code{nrow(visit_data) == nrow(y) * ncol(y)}.}

\item{spatial}{optional spatial spec from \code{tulpa} or \code{tulpaMesh}
(e.g. \code{\link[tulpa:spatial_bym2]{tulpa::spatial_bym2()}}, \code{\link[tulpa:spatial_nngp]{tulpa::spatial_nngp()}}, or a
\link[tulpaMesh:mesh]{tulpaMesh::mesh} object).}

\item{temporal}{optional temporal spec (e.g. \code{\link[tulpa:temporal_ar1]{tulpa::temporal_ar1()}}).}

\item{engine}{inference engine: \code{"auto"}, \code{"laplace"},
\code{"nested_laplace"}, or \code{"nuts"}. \code{"auto"} chooses the family's
\code{default_engine}.}

\item{priors}{optional \code{\link[tulpa:tulpa_priors]{tulpa::tulpa_priors()}} object.}

\item{control}{list of low-level controls (\code{n_threads}, \code{max_iter},
\code{tol}, etc.).}

\item{...}{family-specific named arguments forwarded to the underlying
engine builder.}
}
\value{
An object of class \code{c("tulpa_obs_fit", "<family>_fit", "tulpa_fit")}.
}
\description{
Unified entry point for occupancy, abundance, distance, removal, and
related models that share the latent-state-plus-imperfect-detection
generative template. The specific model is chosen via the \code{family}
argument; the engine (Laplace / nested Laplace / NUTS) via \code{engine}.
}
\details{
This function is the canonical tulpaObs API. The package-specific
entry points (\code{occu()}, \code{occu_fit()}) remain exported for backward
compatibility and are called internally during the Phase 0 transition.
}
\section{Phase 0 status}{


Only \code{family = occ()} is wired to a real engine in v0.1. Other families
raise informative errors pointing to the planned phase in
\code{PLAN_tulpaObs.md}.
}

\examples{
\dontrun{
# Single-season occupancy (currently dispatches to occu())
fit <- tulpa_obs(
  formula   = ~ elev,
  data      = sites,
  family    = occ(),
  detection = ~ effort,
  y         = y_matrix
)
}

}

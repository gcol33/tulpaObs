% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occu_priors.R
\name{occu_priors}
\alias{occu_priors}
\title{Weakly-informative priors for occupancy Laplace fits}
\usage{
occu_priors(
  p_intercept = list(mean = 0, sd = 1.5),
  p_slope = list(mean = 0, sd = 2.5),
  beta_occ_intercept = list(mean = 0, sd = 2),
  beta_occ_slope = list(mean = 0, sd = 5)
)
}
\arguments{
\item{p_intercept}{Prior on the detection intercept (logit scale).
A list \code{list(mean = numeric(1), sd = numeric(1))}.
Default \code{list(mean = 0, sd = 1.5)}.}

\item{p_slope}{Prior on detection slopes (every non-intercept detection
coefficient). Default \code{list(mean = 0, sd = 2.5)}.}

\item{beta_occ_intercept}{Prior on the occupancy (psi / psi1) intercept.
Default \code{list(mean = 0, sd = 2)}.}

\item{beta_occ_slope}{Prior on occupancy slopes. Default
\code{list(mean = 0, sd = 5)}.}
}
\value{
An \code{occu_priors} object, ready to pass to \code{tobs(..., priors = ...)}.
}
\description{
Constructs a prior specification consumed by \code{\link[=tobs]{tobs()}} when
\code{engine = "laplace"} for the occupancy family. The penalty is applied as
a quadratic term \code{+ sum((beta_j - mu_j)^2 / (2 * sd_j^2))} added to the
negative-log-posterior in each M-step submodel block. See
\code{R/penalized_irls.R} for the full derivation.
}
\details{
The defaults are weakly informative: they pull the detection intercept
toward \code{p = 0.5} strongly enough to break the psi-p identifiability
ridge at small \code{J}, but stay wide enough not to bias estimates at
N >= 600 with informative covariates. Setting any \code{sd} to \code{Inf} disables
that component of the prior (same penalised objective, just \code{1/Inf^2 = 0}).
}
\examples{
\dontrun{
# default weakly-informative priors
fit <- tobs(~ x, data = d, family = occu(), detection = ~ z, y = y,
            engine = "laplace", priors = occu_priors())

# disable detection-slope penalty
priors <- occu_priors(p_slope = list(mean = 0, sd = Inf))
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dyn_abun.R
\name{simulate_dyn_abun}
\alias{simulate_dyn_abun}
\title{Simulate Dail-Madsen open N-mixture data}
\usage{
simulate_dyn_abun(
  N = 150,
  T = 4,
  J = 3,
  n_abund_covs = 1,
  beta_lambda = NULL,
  p = 0.5,
  omega = 0.6,
  gamma = 1,
  seed = NULL
)
}
\arguments{
\item{N}{Number of sites (default 150).}

\item{T}{Number of primary seasons (default 4).}

\item{J}{Number of secondary visits per season (default 3).}

\item{n_abund_covs}{Number of initial-abundance covariates (default 1).}

\item{beta_lambda}{Initial-abundance coefficients (log). Default
\code{c(log(5), runif(n_abund_covs, -0.4, 0.4))}.}

\item{p, omega, gamma}{Detection, apparent-survival, and recruitment parameters
(scalars; defaults 0.5, 0.6, 1.0).}

\item{seed}{Optional random seed.}
}
\value{
A list with \code{y} (N x J x T count array), \code{data} (covariates), and
\code{truth} (coefficients, per-site \code{lambda}, scalar \code{p}/\code{omega}/\code{gamma}).
}
\description{
Latent \code{N_1 ~ Poisson(lambda)}; for \code{t >= 2}, `N_t = Binomial(N_(t-1), omega)
\itemize{
\item Poisson(gamma)\verb{; observed via }Binomial(N_t, p)\code{over}J\verb{secondary visits in each of}T\verb{primary seasons. Returns a 3D array}N x J x T` suitable for
\code{\link[=tobs]{tobs()}} with \code{\link[=dyn_abun]{dyn_abun()}}.
}
}

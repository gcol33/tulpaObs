% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{ppc}
\alias{ppc}
\alias{ppc.tobs_fit}
\title{Posterior predictive check}
\usage{
ppc(object, ...)

\method{ppc}{tobs_fit}(object, fit.stat = c("freeman-tukey", "chi-squared"), n.samples = 500, ...)
}
\arguments{
\item{object}{A \code{tobs_fit} object.}

\item{...}{Passed to methods.}

\item{fit.stat}{\code{"freeman-tukey"} (default) or \code{"chi-squared"}.}

\item{n.samples}{Number of posterior samples (default 500).}
}
\value{
A list with \code{fit.y}, \code{fit.y.rep}, and \code{bayesian.p}.
}
\description{
The discrepancy-statistic check (the spOccupancy \code{ppcOcc} construction):
latent occupancy is drawn from its full conditional, a detection replicate
from the fitted model, and the two are compared through a discrepancy
statistic to give a Bayesian p-value. This is a test, not a plot, so it is
its own generic rather than a method on \code{\link[tulpa:pp_check]{tulpa::pp_check()}}, which draws the
graphical check.
}

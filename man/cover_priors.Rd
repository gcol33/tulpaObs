% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occu_priors.R
\name{cover_priors}
\alias{cover_priors}
\title{Weakly-informative priors for cover-hurdle Laplace fits}
\usage{
cover_priors(
  occ_intercept = list(mean = 0, sd = 2),
  occ_slope = list(mean = 0, sd = 2.5),
  pos_intercept = list(mean = 0, sd = 3),
  pos_slope = list(mean = 0, sd = 2.5)
)
}
\arguments{
\item{occ_intercept}{Prior on the occurrence (presence) intercept, logit
scale. \code{list(mean, sd)}. Default \code{list(mean = 0, sd = 2)}.}

\item{occ_slope}{Prior on occurrence slopes. Default
\code{list(mean = 0, sd = 2.5)}.}

\item{pos_intercept}{Prior on the positive-cover intercept (logit scale for
\code{"beta"}, log scale for \code{"lognormal"}). Default \code{list(mean = 0, sd = 3)}.}

\item{pos_slope}{Prior on positive-cover slopes. Default
\code{list(mean = 0, sd = 2.5)}.}
}
\value{
A \code{cover_priors} object, ready to pass to \code{tobs(..., priors = ...)}.
}
\description{
Builds an opt-in fixed-effect prior for \code{\link[=cover]{cover()}} models fit with a Laplace
method. The cover hurdle has two arms with their own coefficients -- an
occurrence arm (Bernoulli presence) and a positive-cover arm (beta or
lognormal) -- each with an intercept and a slope bucket. The penalty is the
quadratic \verb{+ sum((beta_j - mu_j)^2 / (2 sd_j^2))} added to each arm's
negative log-likelihood, the same mechanism \code{\link[=occu_priors]{occu_priors()}} uses.
}
\details{
Cover priors are opt-in: without \code{priors}, a cover fit is unpenalised. The
main use is regularising perfect separation in the occurrence arm at small
\code{N}. Set any \code{sd = Inf} to drop that component.
}
\section{Coverage}{
 Applied on the separate-Laplace path
(\code{method = "laplace"} / \code{"laplace_sla"}) when the formula has no spatial
term. Both arms are penalised: the occurrence and lognormal-positive arms
through \code{\link[tulpa:tulpa_laplace]{tulpa::tulpa_laplace()}}, and the beta-positive arm through
\code{\link[tulpa:tulpa_laplace_beta]{tulpa::tulpa_laplace_beta()}}. The joint nested-Laplace path
(\code{method = "nested_laplace"} / \code{"nested_laplace_sla"}, used for spatial
cover formulas) also threads the priors, as a per-arm \code{beta_prior_mean} /
\code{beta_prior_prec} on the joint engine's occurrence and positive responses.
The separate-Laplace path keeps rejecting a prior alongside a spatial term
(that solver carries its own); add the spatial term through the
nested-Laplace path to combine the two.
}

\examples{
\dontrun{
# regularise the occurrence arm, leave the positive arm unpenalised
fit <- tobs(~ x, data = d, family = cover(response = "lognormal"),
            y = cov, method = "laplace",
            priors = cover_priors(pos_intercept = list(mean = 0, sd = Inf),
                                  pos_slope     = list(mean = 0, sd = Inf)))
}
}
\seealso{
\code{\link[=occu_priors]{occu_priors()}}
}

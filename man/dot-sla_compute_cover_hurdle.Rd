% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sla_cover_hurdle.R
\name{.sla_compute_cover_hurdle}
\alias{.sla_compute_cover_hurdle}
\title{Compute SLA gamma for cover hurdle arms}
\usage{
.sla_compute_cover_hurdle(fits, enc, positive)
}
\arguments{
\item{fits}{The list returned by \code{\link[=fit_cover_hurdle]{fit_cover_hurdle()}} (with \code{m_occ},
\code{m_pos}, \code{positive}, and one of \code{phi_pos} / \code{sigma_pos}).}

\item{enc}{The encoded data from \code{\link[=encode_cover_hurdle]{encode_cover_hurdle()}}.}

\item{positive}{One of \code{"beta"}, \code{"lognormal"}.}
}
\value{
List(gamma_occ, gamma_pos, valid, reason).
}
\description{
Returns per-coefficient SLA gamma for each of the two arms of a cover
hurdle fit. Each arm's gamma is computed via \code{\link[=.sla_gamma_fd]{.sla_gamma_fd()}} using
the arm's \code{solve(H_beta)} as Sigma (no Louis correction — both arms are
real likelihoods at the mode under the single-Laplace path).
}
\details{
phi (beta arm) / sigma_pos (lognormal arm) are treated as fixed nuisance
parameters: SLA gamma is computed only for the fixed-effect coefficients.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obs_families.R
\name{royle_nichols}
\alias{royle_nichols}
\title{Royle-Nichols occupancy family}
\usage{
royle_nichols(K_max = NULL)
}
\arguments{
\item{K_max}{Upper summation bound for the latent abundance (default: a
data-driven Poisson-tail guess).}
}
\value{
A \code{tobs_family} object for \code{\link[=tobs]{tobs()}}.
}
\description{
Occupancy with abundance-induced detection heterogeneity (Royle & Nichols
2003; \pkg{unmarked} \code{occuRN}). Latent abundance \code{N_i ~ Poisson(lambda_i)}
drives the per-visit detection probability \code{1 - (1 - r_i)^{N_i}}, where \code{r_i}
is the per-individual detection probability. The state \code{formula} models
\verb{log lambda} (abundance); \code{detection} models \verb{logit r} at the site level. The
latent \code{N} marginalises in closed form (a Poisson sum to \code{K_max}), so the fit
maximises the exact marginal with an observed-information vcov.
}
\examples{
\donttest{
sim <- simulate_royle_nichols(N = 150, J = 5, seed = 1)
fit <- tobs(~ x, data = sim$data, family = royle_nichols(),
            detection = ~ 1, y = sim$y, control = list(verbose = FALSE))
coef(fit)
}
}

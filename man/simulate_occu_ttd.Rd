% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occu_ttd.R
\name{simulate_occu_ttd}
\alias{simulate_occu_ttd}
\title{Simulate a time-to-detection occupancy data set}
\usage{
simulate_occu_ttd(
  N = 200,
  J = 4,
  n_psi_covs = 1,
  n_rate_covs = 1,
  beta_psi = NULL,
  beta_rate = NULL,
  Tmax = 3,
  seed = NULL
)
}
\arguments{
\item{N}{Number of sites (default 200).}

\item{J}{Number of replicate surveys per site (default 4).}

\item{n_psi_covs, n_rate_covs}{Number of occupancy / rate covariates.}

\item{beta_psi}{Occupancy coefficients \code{c(intercept, slopes...)} (logit).
Default \code{c(qlogis(0.6), runif(n_psi_covs, -0.5, 0.5))}.}

\item{beta_rate}{Log detection-rate coefficients \code{c(intercept, slopes...)}.
Default \code{c(log(0.6), runif(n_rate_covs, -0.4, 0.4))}.}

\item{Tmax}{Survey length (scalar). Default 3.}

\item{seed}{Optional random seed.}
}
\value{
A list with \code{y} (N x J TTD matrix), \code{data}, \code{Tmax}, and \code{truth}.
}
\description{
Draws from the \code{\link[=occu_ttd]{occu_ttd()}} model: site occupancy \code{z ~ Bernoulli(psi)} and,
at occupied sites, exponential time-to-detection at rate \code{lambda} over a
survey of length \code{Tmax} (a non-detection is censored at \code{Tmax}).
}

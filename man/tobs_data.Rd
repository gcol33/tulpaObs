% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{tobs_data}
\alias{tobs_data}
\title{Convert long-format data to a site x visit observation object}
\usage{
tobs_data(
  df,
  y,
  site,
  visit,
  type = c("occurrence", "abundance", "cover", "positive"),
  occ.covs = NULL,
  det.covs = NULL,
  coords = NULL,
  sites = NULL,
  visits = NULL,
  cover.floor = 0,
  compact = FALSE
)
}
\arguments{
\item{df}{Data.frame in long format (one row per site-visit).}

\item{y}{Character, name of the response column. Its meaning is set by
\code{type}: a 0/1 detection (\code{"occurrence"}), an integer count
(\code{"abundance"}), or a continuous cover proportion in \verb{[0, 1]} (\code{"cover"}).}

\item{site}{Character, name of site identifier column.}

\item{visit}{Character, name of visit/replicate column.}

\item{type}{Response kind. \code{"occurrence"} (default) and \code{"abundance"} build
an integer site x visit matrix; \code{"cover"} and \code{"positive"} build a double
matrix and preserve continuous values (no coercion to integer). \code{"cover"}
is a proportion in \verb{[0, 1]} (the beta cover arm); \code{"positive"} is a
positive real \verb{(0, Inf)} (the lognormal / gamma cover arm), validated as
non-negative with no upper bound. Both apply the \code{cover.floor} absence
policy below.}

\item{occ.covs}{Character vector of site-level covariate names.}

\item{det.covs}{Character vector of visit-level covariate names. A named
column that is a factor or character is preserved as categorical: the
downstream detection / positive design expands it to k - 1 dummies for a
k-level factor, with the factor's first level (for a character column, the
first level after sorting the unique values) as the reference. Numeric
columns are kept as a continuous covariate.}

\item{coords}{Character vector of length 2 for coordinate columns.}

\item{sites}{Optional site level set. When supplied, the site x visit grid
uses these site identifiers in this order (rather than \code{df}'s
first-appearance order), so a subset of \code{df} pivots onto a fixed,
externally-defined site grid; site values in \code{df} outside the set error.}

\item{visits}{Optional visit level set, analogous to \code{sites} for the visit
axis (default the sorted unique visits in \code{df}).}

\item{cover.floor}{For \code{type = "cover"} / \code{"positive"}, the threshold at or
below which a value is stored as \code{NA} rather than as a positive observation
(default \code{0}). The positive arm of a hurdle is positive-only, so a \code{0} is
an absence handled by the occurrence arm, not a positive observation;
sending it to \code{NA} keeps a sampled-absent or unsampled cell from entering
the positive arm as a fabricated zero (which, padded across a grid,
flattens the spatial field). Set \code{cover.floor = -Inf} to keep every value
verbatim.}

\item{compact}{Logical (default \code{FALSE}). When \code{TRUE}, return a compact
(ragged) \code{tobs_data}: the response is stored as one row per valid
site-visit (a \code{tobs_ragged} carrier) rather than as a padded
\verb{[n_sites x max_visits]} matrix, and each detection covariate as a
length-V vector in the same canonical \code{order(site, visit)}. The compact
layout has no per-site visit cap (its memory is the number of observations,
not the padded grid) and is consumed by the joint nested-Laplace
\code{occu_cover()} engine, which works one valid visit at a time. Two compact
calls on the same \code{df} / \code{site} / \code{visit} align row-for-row, so an
occurrence response and a cover response can be paired directly.}
}
\value{
An \code{tobs_data} object. With \code{compact = TRUE} its \verb{$y} is a
\code{tobs_ragged} carrier and its \verb{$det.covs} are length-V vectors.
}
\description{
Convert long-format data to a site x visit observation object
}

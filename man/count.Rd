% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obs_families.R
\name{count}
\alias{count}
\title{Count / relative-abundance GLMM family (no detection process)}
\usage{
count(response = c("poisson", "negbin", "gaussian", "binomial"))
}
\arguments{
\item{response}{The response distribution: \code{"poisson"} (log link),
\code{"negbin"} (negative binomial, log link, an estimated size / dispersion),
\code{"gaussian"} (identity link, an estimated residual variance), or
\code{"binomial"} (logit link, \code{k} successes out of \code{n} trials per site). The
binomial response is the detection-free binomial GLMM of \code{spOccupancy}
(\code{svcPGBinom}): supply the per-site trial count as \verb{trials =} on \code{\link[=tobs]{tobs()}}
(default 1, i.e. a Bernoulli response). Unlike the count / Gaussian areal
fit, a binomial areal field is identified (the variance is pinned by \code{n}),
so an \code{icar()} / \code{car_proper()} term composes with any \code{trials}.}
}
\value{
A \code{tobs_family} object.
}
\description{
A generalized linear model on an observed count (or continuous) response
directly, with no detection sub-model and no latent abundance to marginalize.
The abundance analogue of \code{\link[=jsdm]{jsdm()}}: where \code{\link[=abun]{abun()}} fits Royle's N-mixture
(latent \code{N} plus imperfect detection), \code{count()} fits the observed response
as a plain GLMM. This is the "relative abundance" model of \code{spAbundance}
(\code{abund}): one value per site, \code{log}-link Poisson / negative-binomial counts
or an identity-link Gaussian response.
}
\details{
The response is a numeric vector (one value per site), supplied via \verb{y =} or
on a two-sided \code{formula} left-hand side (\code{count.value ~ predictors}).
}
\examples{
f <- count("poisson")
f
}
\seealso{
\code{\link[=abun]{abun()}} (N-mixture: latent abundance + detection), \code{\link[=jsdm]{jsdm()}}
(occurrence GLMM, no detection).
}

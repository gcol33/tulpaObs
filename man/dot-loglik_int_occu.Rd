% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sla_int_occu.R
\name{.loglik_int_occu}
\alias{.loglik_int_occu}
\title{Integrated multi-source occupancy log-likelihood (R-side)}
\usage{
.loglik_int_occu(beta, model)
}
\arguments{
\item{beta}{Joint parameter vector in process_info order:
\code{c(beta_psi, beta_det_1, beta_det_2, ..., beta_det_S)}.}

\item{model}{A \code{tobs_model} with \code{model_type = "integrated"}.}
}
\value{
Scalar log-likelihood.
}
\description{
Computes the marginal observation log-likelihood
\verb{log P(y | beta) = Sum_i log( psi_i * Prod_s P(y_is | z=1, p_is)
+ (1 - psi_i) * I(no detections at any source) )}
where \verb{psi_i = plogis(X_occ[i,] beta_psi)},
\verb{p_is = plogis(X_det_s[i,] beta_det_s)}.
}
\details{
Reads \code{model$y_sources} (list of integer matrices, source-local row indexing,
NA encoded as -1), \code{model$site_maps} (0-indexed global site per source row),
\code{model$X_processes} (occ design + one det design per source, all n_sites x p),
\code{model$n_sources}, \code{model$n_sites}. No priors, no pseudo-binomial encoding.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\docType{data}
\name{foray_counts}
\alias{foray_counts}
\title{Repeated point-count abundance survey (synthetic)}
\format{
A list with components:
\describe{
\item{y}{100 x 3 integer matrix of counts.}
\item{occ.covs}{Data frame of site covariates: \code{elevation}, \code{shrub} (both
standardised).}
\item{det.covs}{Named list with one 100 x 3 visit covariate matrix,
\code{effort} (standardised).}
\item{truth}{List of generative quantities: \code{beta_lambda}, \code{beta_p}, the
realised latent abundance \code{N}, and per-site \code{lambda}.}
}
}
\usage{
foray_counts
}
\description{
A binomial N-mixture dataset (Royle 2004) of repeated counts at 100 sites
over 3 visits, drawn from a known generative model. Latent abundance
increases with \code{shrub} cover and decreases with \code{elevation}; per-visit
detection increases with survey \code{effort}. Synthetic, not field data; the
generative coefficients and the realised latent abundances are in \code{truth}.
}
\examples{
data(foray_counts)
summary(as.vector(foray_counts$y))
\donttest{
fit <- tobs(~ elevation + shrub, data = foray_counts$occ.covs,
            family = abun(), detection = ~ effort,
            y = foray_counts$y, visits = foray_counts$det.covs)
coef(fit)
}
}
\seealso{
\code{\link[=abun]{abun()}}, \code{\link[=tobs]{tobs()}}, \code{\link[=simulate_abun]{simulate_abun()}}
}
\keyword{datasets}

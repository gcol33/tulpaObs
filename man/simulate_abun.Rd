% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abun.R
\name{simulate_abun}
\alias{simulate_abun}
\title{Simulate Royle (2004) N-mixture abundance data}
\usage{
simulate_abun(
  N = 100,
  J = 4,
  n_abund_covs = 2,
  n_det_covs = 1,
  beta_lambda = NULL,
  beta_p = NULL,
  mixture = c("poisson", "negbin", "zip", "zinb"),
  size = 2,
  omega = 0.3,
  seed = NULL
)
}
\arguments{
\item{N}{Number of sites (default 100).}

\item{J}{Number of replicate visits (default 4).}

\item{n_abund_covs}{Number of abundance covariates (default 2).}

\item{n_det_covs}{Number of detection covariates (default 1).}

\item{beta_lambda}{Abundance coefficients \code{c(intercept, slopes...)} on the
log scale. Default \code{c(log(3), runif(n_abund_covs, -0.5, 0.5))}.}

\item{beta_p}{Detection coefficients \code{c(intercept, slopes...)} on the logit
scale. Default \code{c(0, runif(n_det_covs, -0.5, 0.5))} (intercept 0 = p 0.5).}

\item{mixture}{Abundance mixing distribution: \code{"poisson"} (default),
\code{"negbin"} (negative binomial, overdispersed), or their zero-inflated
counterparts \code{"zip"} / \code{"zinb"} (a structural-zero share \code{omega} of sites
have \code{N = 0} regardless of \code{lambda}).}

\item{size}{Negative-binomial size \code{r} (the \code{"negbin"} / \code{"zinb"} dispersion;
variance \code{lambda + lambda^2 / r}). Smaller \code{r} means more overdispersion;
as \code{r} grows large the model approaches Poisson. Default 2. Ignored under
Poisson.}

\item{omega}{Structural-zero probability for \code{"zip"} / \code{"zinb"} (the share of
sites with \code{N = 0} independent of \code{lambda}). Default 0.3. Ignored otherwise.}

\item{seed}{Optional random seed.}
}
\value{
A list with \code{y} (N x J count matrix), \code{data} (covariate data frame),
and \code{truth} (the coefficients, per-site \code{lambda}, \code{p}, latent \code{N}, and the
\code{mixture} / \code{size} used).
}
\description{
Latent abundance \code{N_i ~ Poisson(lambda_i)} (or, under
\code{mixture = "negbin"}, \code{N_i ~ NegBin(mean = lambda_i, size = size)} with
variance \code{lambda + lambda^2 / size}) with \verb{log lambda_i = X_lambda beta_lambda}, and replicate counts \code{y_ij ~ Binomial(N_i, p_i)} with
\verb{logit p_i = X_p beta_p} (site-level detection). The returned \code{y} is an
\verb{N x J} integer count matrix suitable for \code{\link[=tobs]{tobs()}} with \code{\link[=abun]{abun()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obs_families.R
\name{gdistremoval}
\alias{gdistremoval}
\title{Joint distance + removal sampling family}
\usage{
gdistremoval(transect = c("line", "point"), cutpoints = NULL)
}
\arguments{
\item{transect}{Transect geometry: \code{"line"} (default) or \code{"point"}.}

\item{cutpoints}{Distance-bin edges, length \code{ncol(y) + 1}, strictly increasing
and starting at \verb{>= 0}.}
}
\value{
A \code{tobs_family} object.
}
\description{
The \code{unmarked} \code{gdistremoval} model (Amundson et al. 2014): a \strong{single-season}
point-count design in which the detected individuals are recorded two ways at
once -- their distance band (distance sampling) and the removal period of first
detection (removal sampling). This is NOT the open-population distance model
(\code{distsampOpen}); the abundance is static.
}
\details{
Site abundance \code{N_i ~ Poisson(lambda_i)}; the detected birds are
cross-classified by a distance band and a removal period. Writing \code{pdist_i} for
the overall distance detection, \code{prem_i} for the overall removal detection, the
total detected is a binomial thinning of \code{N_i}, and Poisson is closed under
binomial thinning, so the marginal is closed-form:
\deqn{\mathrm{ysum}_i \sim \mathrm{Poisson}(\lambda_i\, p^{dist}_i\, p^{rem}_i),}
with the distance-band counts and removal-period counts as two conditional
multinomials -- the \code{\link[=double_observer]{double_observer()}} Poisson-multinomial pattern, here with
a distance multinomial (half-normal key band integrals) and a depleting-removal
multinomial \code{pi_k = r (1 - r)^{k-1}}.

Three site-level arms: log abundance \code{lambda} (the \code{\link[=tobs]{tobs()}} \code{formula}), log
distance scale \code{sigma} (\code{detection}), and logit per-period removal capture \code{r}
(\code{removal = ~ ...}, default intercept-only).
}
\section{Inputs}{

\code{y} is an \verb{n_sites x n_bins} integer matrix of per-distance-band counts;
\code{y_rem} an \verb{n_sites x n_periods} integer matrix of per-removal-period counts.
The per-site row totals must match (the same detected birds cross-classified).
}

\references{
Amundson, C. L., Royle, J. A., Handel, C. M. (2014). A hierarchical model
combining distance sampling and time removal to estimate detection
probability during avian point counts. \emph{The Auk} 131, 476-494.
}
\seealso{
\code{\link[=distance]{distance()}}, \code{\link[=removal]{removal()}}, \code{\link[=double_observer]{double_observer()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ms_count.R
\name{ms_count}
\alias{ms_count}
\title{Multispecies (community) relative-abundance GLMM family}
\usage{
ms_count(response = c("poisson", "negbin", "gaussian", "binomial"))
}
\arguments{
\item{response}{One of \code{"poisson"}, \code{"negbin"}, \code{"gaussian"}, or
\code{"binomial"}. The binomial response is the community \code{k}-of-\code{n} GLMM
(community \code{svcPGBinom}): supply the per-site (or per-\verb{site x species})
trial count as \verb{trials =} on \code{\link[=tobs]{tobs()}} (default 1, i.e. Bernoulli, which is
the \code{\link[=jsdm]{jsdm()}} response). With \code{trials > 1} the community-mean intercept
carries a small first-order-Laplace bias of order \code{1 / n_species} (a few
hundredths on the logit scale at 20 species, shrinking with more species;
the slope and the \code{trials = 1} case are unbiased) -- the same character as
the negative-binomial slope attenuation noted above.}
}
\value{
A \code{tobs_family} object.
}
\description{
The community analogue of \code{\link[=count]{count()}}: a per-species GLMM on an observed count
or continuous response with Gaussian community hyperpriors on the per-species
coefficients (the spAbundance \code{msAbund} model). No detection and no latent
state -- the abundance counterpart of \code{\link[=ms_occu]{ms_occu()}} without the occupancy layer.
Poisson / negative-binomial (log link) or Gaussian (identity). \code{y} is an
\verb{n_sites x n_species} matrix (or a named list of \code{n_species} count vectors),
one value per site per species; \code{species} names the columns.
}
\details{
As with the other community Laplace-EM families (\code{\link[=ms_occu]{ms_occu()}}, \code{\link[=ms_occu_cover]{ms_occu_cover()}}),
the community means are recovered essentially unbiased for the Gaussian
(identity link, exact Laplace) and Poisson responses; the negative-binomial
slope carries a mild first-order-Laplace (PQL) attenuation of order the
community variance (a few percent, shrinking with the number of species and
observations per species). The community-mean Wald intervals are calibrated to
the package rubric (pooled coverage at least 0.85).

\code{method = "nuts"} samples the exact joint posterior (community means,
per-species deviations, and the community covariance) for all three responses
-- the negative binomial carrying a per-species dispersion random effect, the
Gaussian a per-species free residual variance -- which removes the
Laplace-EM's negative-binomial attenuation and returns calibrated,
non-Gaussian community intervals. A shared areal field or latent factors are
available through \code{method = "nested_laplace"} / the \code{latent()} term; see the
package overview for the spatial and factor variants.
}
\examples{
\donttest{
sim <- simulate_ms_count(N = 120, n_species = 8, seed = 1)
fit <- tobs(~ x, data = sim$data, family = ms_count(), y = sim$y,
            species = colnames(sim$y), method = "laplace")
summary(fit)
}
}
\seealso{
\code{\link[=count]{count()}} (single species), \code{\link[=ms_occu]{ms_occu()}}, \code{\link[=ms_abun]{ms_abun()}}
}

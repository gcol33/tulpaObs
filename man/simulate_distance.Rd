% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance.R
\name{simulate_distance}
\alias{simulate_distance}
\title{Simulate binned distance-sampling abundance data}
\usage{
simulate_distance(
  N = 200,
  cutpoints = seq(0, 1, length.out = 6),
  key = c("halfnorm", "hazard"),
  transect = c("line", "point"),
  n_abund_covs = 1,
  n_sigma_covs = 1,
  beta_lambda = NULL,
  beta_sigma = NULL,
  shape = 3,
  mixture = c("poisson", "negbin"),
  size = 5,
  seed = NULL
)
}
\arguments{
\item{N}{Number of sites (default 200).}

\item{cutpoints}{Distance-bin edges (length \code{n_bins + 1}). Default
\code{seq(0, 1, length.out = 6)} (five bins out to 1).}

\item{key}{\code{"halfnorm"} (default) or \code{"hazard"}.}

\item{transect}{\code{"line"} (default) or \code{"point"}.}

\item{n_abund_covs, n_sigma_covs}{Number of abundance / detection covariates.}

\item{beta_lambda}{Abundance coefficients (log scale). Default
\code{c(log(40), runif(n_abund_covs, -0.4, 0.4))}.}

\item{beta_sigma}{Detection-scale coefficients (log scale). Default
\code{c(log(0.4), runif(n_sigma_covs, -0.3, 0.3))}.}

\item{shape}{Hazard-rate shape \code{b} (\code{key = "hazard"} only, default 3).}

\item{mixture}{\code{"poisson"} (default) or \code{"negbin"}.}

\item{size}{Negative-binomial size \code{r} (\code{mixture = "negbin"} only, default 5).}

\item{seed}{Optional random seed.}
}
\value{
A list with \code{y} (N x n_bins count matrix), \code{data} (covariates),
\code{cutpoints}, and \code{truth} (coefficients, per-site \code{lambda} / \code{sigma}, latent
\code{N}, key/transect/mixture/shape/size).
}
\description{
Latent abundance \code{N_i ~ Poisson(lambda_i)} (or \code{NegBin(lambda_i, size)}) with
\verb{log lambda_i = X_lambda beta_lambda}, observed through a half-normal or
hazard-rate detection function with \verb{log sigma_i = X_sigma beta_sigma}. Each
individual lands in a distance bin (or goes undetected) by the multinomial
cell probabilities. Returns an \verb{N x n_bins} integer matrix of per-bin counts
suitable for \code{\link[=tobs]{tobs()}} with \code{\link[=distance]{distance()}}.
}

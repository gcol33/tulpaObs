% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fp_occu.R
\name{fp_occu_laplace}
\alias{fp_occu_laplace}
\title{Maximum-likelihood fit of the multistate false-positive occupancy model}
\usage{
fp_occu_laplace(
  y,
  site_idx,
  X_psi,
  X_p11,
  X_p10,
  X_b,
  sigma_beta = NULL,
  max_iter = 200L,
  tol = 1e-08,
  verbose = FALSE
)
}
\arguments{
\item{y}{Integer vector of detection states (\code{0}/\code{1}/\code{2}), long form (valid
visits only).}

\item{site_idx}{Integer vector, same length as \code{y}, 1-based site index.}

\item{X_psi, X_p11, X_p10, X_b}{Numeric \verb{[n_sites x p_arm]} design matrices for
the four arms.}

\item{sigma_beta}{Optional Gaussian prior SD on the coefficients (a mild ridge
for stability); \code{NULL} (default) is the unpenalised MLE.}

\item{max_iter}{BFGS iteration budget (default 200).}

\item{tol}{Convergence tolerance (\code{optim} \code{reltol}, default 1e-8).}

\item{verbose}{Print convergence status.}
}
\value{
A list of class \code{fp_occu_fit} with \code{beta_psi}, \code{beta_p11},
\code{beta_p10}, \code{beta_b}, \code{log_lik}, \code{vcov}, \code{H_obs}, per-site posterior
occupancy \code{w1}, and \code{converged}.
}
\description{
Fits the Miller et al. (2011) false-positive occupancy model with confirmed
detections (\code{y} in \verb{\{0, 1, 2\}}) by maximising the exact two-state marginal
likelihood with an analytic gradient (BFGS). The observed-information
covariance is the inverse of the negative finite-difference Jacobian of the
analytic gradient at the mode. All four arms (occupancy \code{psi}, true detection
\code{p11}, false-positive \code{p10}, certain-classification \code{b}) are site-level logit
predictors.
}
\references{
Miller, D. A. W., et al. (2011). Improving occupancy estimation when two types
of observational error occur. \emph{Ecology} 92, 1422-1428.
}

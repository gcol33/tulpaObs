% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obs_families.R
\name{occu_categorical}
\alias{occu_categorical}
\title{Presence + nominal class hurdle family}
\usage{
occu_categorical(classes = NULL)
}
\arguments{
\item{classes}{optional character vector of class labels (length \code{K}), used
only to name the coefficient blocks; when \code{NULL}, \code{K} is taken from
\code{max(y)} and the classes are labelled \verb{1..K}.}
}
\value{
A \code{tobs_family} object.
}
\description{
A hurdle for a response that is either absent or, when present, one of \code{K}
\strong{nominal (unordered)} classes -- a colour morph, microhabitat / substrate
use, or a cryptic-species / classifier label observed given the organism is
present. The presence/absence part is a Bernoulli arm; the class given present
is a baseline-category multinomial logit (the last class is the baseline). The
two arms factorise the likelihood exactly:
\deqn{P(y = 0) = 1 - \psi, \qquad P(y = k) = \psi \, p_k,}
with \eqn{p = \mathrm{softmax}(X \beta_{class})}.
}
\details{
This is the categorical counterpart of \code{\link[=cover]{cover()}} (presence + a magnitude):
here the positive part is an unordered class, so it uses a multinomial logit
rather than beta / lognormal. For an \emph{ordered} class response (Braun-Blanquet
cover bands) use \code{cover(response = "ordinal")}, which exploits the ordering;
this family is for classes with no ordering.
}
\section{Response}{

\code{y} is a length-N integer vector in \verb{0..K}: \code{0} is absent, \code{k} is class \code{k}.
It may sit on the formula left-hand side (\code{y ~ ...}), so \verb{y =} can be dropped.
The \code{formula} predictors are shared by both arms.
}

\section{Scope}{

Non-spatial Laplace (\code{method = "laplace"}). The multinomial math is the
FD-validated tulpa kernel (\code{multinomial_logit.h}); the non-spatial fit is the
vectorised R Newton over the same closed forms. Spatial fields / NUTS (the
native multi-process likelihood) and the latent-class \emph{misclassification}
variant (the K-class generalisation of \code{\link[=fp_occu]{fp_occu()}}, a confusion matrix on the
observed label) are documented follow-ups (gcol33/tulpaObs#106).
}

\seealso{
\code{\link[=cover]{cover()}} (presence + magnitude), \code{\link[=fp_occu]{fp_occu()}} (two-state
false-positive detection).
}

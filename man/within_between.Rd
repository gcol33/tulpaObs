% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/within_between.R
\name{within_between}
\alias{within_between}
\title{Within / between (Mundlak) decomposition for longitudinal data}
\usage{
within_between(data, group, vars, suffix = c("_btw", "_wtn"), na.rm = TRUE)
}
\arguments{
\item{data}{A data frame.}

\item{group}{Character name (or character vector of names) identifying the
grouping column(s). With multiple names, grouping is by the interaction.}

\item{vars}{Character vector of numeric columns to decompose. Must all
exist in \code{data}.}

\item{suffix}{Length-2 character vector giving the suffixes for the
between- and within-group columns. Default \code{c("_btw", "_wtn")}.}

\item{na.rm}{Logical; passed to \code{mean()} when computing per-group means.
Default \code{TRUE}.}
}
\value{
The input \code{data} with \code{2 * length(vars)} new columns appended.
}
\description{
For each variable in \code{vars}, adds two new columns to \code{data}:
}
\details{
\itemize{
\item \verb{<var><suffix[1]>} - the per-group mean of \code{var} (the \strong{between}-group
component, constant within each group),
\item \verb{<var><suffix[2]>} - \verb{var - <var><suffix[1]>} (the \strong{within}-group
deviation, summing to zero within each group).
}

The decomposition is exact: \code{data[[var]] == data[[btw]] + data[[wtn]]}.
Including both columns in a regression (\code{~ x_btw + x_wtn}) separates the
cross-group association from the within-group association without imposing
that they be the same coefficient (the random-effects assumption).

Typical use in the MOTIVATE cover-resurvey workflow:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{plots <- within_between(plots, group = "plot_id", vars = "year")
fit   <- tobs(~ year_btw + year_wtn + bym2(graph = adj), data = plots,
              family = cover("beta"), y = plots$cover,
              method = "nested_laplace")
}\if{html}{\out{</div>}}

\code{year_wtn} then carries the within-plot temporal trend; \code{year_btw} absorbs
across-plot timing heterogeneity (older versus newer baseline plots).
}
\examples{
set.seed(1)
plots <- data.frame(
  plot_id = rep(1:5, each = 4),
  year    = rep(2000:2003, times = 5) + rep(c(0, 5, 10, 15, 20), each = 4),
  cover   = stats::runif(20)
)
decomposed <- within_between(plots, group = "plot_id", vars = "year")
head(decomposed)
}
\references{
Mundlak, Y. (1978). On the pooling of time series and cross section data.
\emph{Econometrica}, 46(1), 69-85.

Bell, A. & Jones, K. (2015). Explaining fixed effects: random effects
modeling of time-series cross-sectional and panel data.
\emph{Political Science Research and Methods}, 3(1), 133-153.
}

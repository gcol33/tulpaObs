% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occu_cover_simulate.R
\name{simulate_occu_cover}
\alias{simulate_occu_cover}
\title{Simulate joint occupancy-detection + cover data}
\usage{
simulate_occu_cover(
  N = 200L,
  J = 4L,
  n_occ_covs = 1L,
  n_det_covs = 1L,
  n_pos_covs = 1L,
  beta_occ = NULL,
  beta_p = NULL,
  beta_pos = NULL,
  positive = c("lognormal", "beta"),
  phi = 30,
  sigma_pos = 0.4,
  adj = NULL,
  sigma = 0.6,
  alpha = 1,
  trend = FALSE,
  sigma_trend = 0.6,
  alpha_trend = 1,
  pos_field = FALSE,
  sigma_pos_int = 0.5,
  sigma_pos_trend = 0.6,
  det_field = FALSE,
  sigma_p_int = 0.5,
  sigma_p_trend = 0.6,
  re_det_groups = NULL,
  sigma_re_p = 0.7,
  re_det = NULL,
  seed = NULL
)
}
\arguments{
\item{N}{Number of sites (cells).}

\item{J}{Number of visits per site.}

\item{n_occ_covs, n_det_covs, n_pos_covs}{Number of covariates on each arm
(drawn IID standard normal).}

\item{beta_occ, beta_p, beta_pos}{Coefficient vectors c(intercept, slopes).
Defaults pick weakly-informative values: psi intercept at logit(0.4),
p intercept at logit(0.5), cover intercept on the appropriate link.}

\item{phi}{Beta precision when \code{positive = "beta"} (default 30).}

\item{sigma_pos}{Lognormal residual SD when \code{positive = "lognormal"}
(default 0.4).}

\item{adj}{Optional N x N adjacency matrix. When supplied, generates the
shared ICAR field; when NULL, the simulator is non-spatial (matches v1).}

\item{sigma}{Spatial field amplitude (used only when \code{adj} is supplied).}

\item{alpha}{Cover-arm scaling on the shared field (used only when \code{adj}
is supplied). 1.0 = arms see the field identically; positive = same sign,
negative = opposite.}

\item{trend}{Logical; when \code{TRUE} (and \code{adj} is supplied) a SECOND shared
ICAR field \code{f2} (a spatially-varying temporal trend) is generated on the
same graph, weighted by a per-cell covariate \code{time} drawn IID standard
normal. The trend enters the occupancy and cover predictors as
\code{sigma_trend * time_i * f2[i]} (occupancy) and
\code{alpha_trend * sigma_trend * time_i * f2[i]} (cover); the detection
predictor is unaffected. The \code{time} covariate is per-cell and broadcast
to every visit of that cell.}

\item{sigma_trend}{Trend-field amplitude (used only when \code{trend = TRUE}).}

\item{alpha_trend}{Cover-arm scaling on the trend field (used only when
\code{trend = TRUE}).}

\item{pos_field}{Logical; when \code{TRUE} (and \code{adj} is supplied) draw an
INDEPENDENT areal field on the cover (positive) arm only -- an intercept
field plus a time-weighted trend field, each unrelated to the occupancy
field and with no alpha copy (gcol33/tulpaObs#110). Adds a \code{time} column to
the returned \code{data} and reports \code{g0} / \code{g1} (the two fields) and their SDs in
\code{truth}. Fit by placing \code{spatial(~ 1 + time || cell, graph = adj)} in the
\code{positive} formula.}

\item{sigma_pos_int}{Cover-arm intercept-field SD (used only when
\code{pos_field = TRUE}).}

\item{sigma_pos_trend}{Cover-arm trend-field SD (used only when
\code{pos_field = TRUE}).}

\item{re_det_groups}{Optional integer \verb{>= 2}: the number of levels of a
per-visit detection random intercept (a \code{habitat} factor on \code{visit_data},
levels \code{hab1..K}), drawn \code{b_g ~ N(0, sigma_re_p^2)} and centred. \code{NULL}
(default) adds no detection RE. Recover it with
\code{detection = ~ ... + (1 | habitat)}.}

\item{sigma_re_p}{SD of the \code{re_det_groups} random intercept (default 0.7).}

\item{re_det}{Optional named list of FURTHER per-visit detection random
effects, for crossed / nested / slope designs. Each element
\code{list(K =, sigma =, prefix =, nested_in =, slope_cov =, sigma_slope =, rho =)}
adds a factor column (levels \verb{<prefix>1..K}). Without \code{slope_cov} it is a
centred \code{N(0, sigma^2)} random intercept; \code{nested_in = "<name>"} nests its
codes within a previously listed grouping (matching \code{(1 | parent/child)}),
otherwise crossed. With \code{slope_cov = "<column>"} (a per-visit covariate,
generated \code{N(0, slope_sd)} if absent, \code{slope_sd} default 1) it is a random
slope: a slope-only uncorrelated block when \code{rho} is unset, or a correlated
intercept + slope block (covariance from \code{sigma} / \code{sigma_slope} / \code{rho})
when \code{rho} is given.
Truth is returned in \code{truth$re_det[[name]]} (named by the level label a fit
reconstructs): \code{b} / \code{b_slope} BLUP vectors, or the \code{B} BLUP matrix plus
\code{s0} / \code{s1} / \code{rho} for a correlated slope.}

\item{seed}{Optional integer seed.}

\item{response}{\code{"beta"} or \code{"lognormal"}.}
}
\value{
A list with \code{y} (N x J detection matrix), \code{y_pos} (N x J cover
matrix, NA where not detected), \code{data} (per-site covariate frame, gaining
a \code{time} column when \code{trend = TRUE}), \code{visit_data} (per-visit covariate
frame, N*J rows in site-major order), and \code{truth} (the coefficients,
dispersion, and field(s) if generated; \code{f2}, \code{sigma_trend}, \code{alpha_trend},
and \code{time} when \code{trend = TRUE}).
}
\description{
Per-cell mixture: latent z_i ~ Bernoulli(psi_i), per-visit detection
y_ij | z_i = 1 ~ Bernoulli(p_ij), per-visit cover y_pos_ij | y_ij = 1
drawn from \code{positive} (\code{"beta"} or \code{"lognormal"}) on the cover-arm linear
predictor. Used by the recovery test and as the generator for the
joint occupancy + cover hurdle family (see \code{\link[=occu_cover]{occu_cover()}}).
}
\details{
When \code{adj} is supplied (a square adjacency matrix), an ICAR field
\verb{f[1..N]} is drawn from \verb{MVN(0, Q^-)} (with sum-to-zero constraint),
and the linear predictors become

\if{html}{\out{<div class="sourceCode">}}\preformatted{eta_psi_i = X_psi[i, ] \%*\% beta_occ + sigma * f[i]
eta_pos_ij = X_pos[i, ] \%*\% beta_pos + alpha * sigma * f[i]
}\if{html}{\out{</div>}}

matching the v2 nested-Laplace fit's parameterisation.
}

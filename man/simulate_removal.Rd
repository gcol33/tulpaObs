% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/removal.R
\name{simulate_removal}
\alias{simulate_removal}
\title{Simulate removal-sampling abundance data}
\usage{
simulate_removal(
  N = 100,
  K = 4,
  n_abund_covs = 2,
  n_det_covs = 1,
  beta_lambda = NULL,
  beta_p = NULL,
  mixture = c("poisson", "negbin"),
  size = 3,
  seed = NULL
)
}
\arguments{
\item{N}{Number of sites (default 100).}

\item{K}{Number of removal passes (default 4).}

\item{n_abund_covs}{Number of abundance covariates (default 2).}

\item{n_det_covs}{Number of detection covariates (default 1).}

\item{beta_lambda}{Abundance coefficients on the log scale. Default
\code{c(log(6), runif(n_abund_covs, -0.5, 0.5))}.}

\item{beta_p}{Detection coefficients on the logit scale. Default
\code{c(0.4, runif(n_det_covs, -0.5, 0.5))}.}

\item{mixture}{\code{"poisson"} (default) or \code{"negbin"}.}

\item{size}{Negative-binomial size \code{r} (\code{mixture = "negbin"} only, default 3).}

\item{seed}{Optional random seed.}
}
\value{
A list with \code{y} (N x K removal matrix), \code{data} (covariate data frame),
and \code{truth} (coefficients, per-site \code{lambda}, \code{p}, latent \code{N}, mixture/size).
}
\description{
Latent abundance \code{N_i ~ Poisson(lambda_i)} (or \code{NegBin(lambda_i, size)}) with
\verb{log lambda_i = X_lambda beta_lambda}, observed through \code{K} ordered removal
passes: pass \code{k} removes \verb{Binomial(N_i - sum_\{l<k\} y_\{il\}, p_\{ik\})} of the
individuals still present, with \verb{logit p_\{ik\} = X_p beta_p} (site-level
detection here). The returned \code{y} is an \verb{N x K} integer matrix of per-pass
removals suitable for \code{\link[=tobs]{tobs()}} with \code{\link[=removal]{removal()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dyn_abun.R
\name{dyn_abun_laplace}
\alias{dyn_abun_laplace}
\title{Maximum-likelihood fit of the Dail-Madsen open N-mixture}
\usage{
dyn_abun_laplace(
  y_flat,
  n_sites,
  T,
  J,
  K_max,
  X_lambda,
  X_p,
  X_omega,
  X_gamma,
  max_iter = 300L,
  tol = 1e-08,
  verbose = FALSE
)
}
\arguments{
\item{y_flat}{Integer vector, flattened detection counts (site-major, then
season, then visit; \code{-1} marks a missing visit).}

\item{n_sites, T, J}{Sites, primary seasons, secondary visits per season.}

\item{K_max}{Abundance-state truncation (states \verb{0..K_max}).}

\item{X_lambda, X_p, X_omega, X_gamma}{Numeric \verb{[n_sites x p_arm]} design matrices.}

\item{max_iter}{BFGS iteration budget (default 300).}

\item{tol}{Convergence tolerance (\code{optim} \code{reltol}, default 1e-8).}

\item{verbose}{Print convergence status.}
}
\value{
A list of class \code{dyn_abun_fit} with \code{beta_lambda}, \code{beta_p},
\code{beta_omega}, \code{beta_gamma}, \code{log_lik}, \code{vcov}, \code{H_obs}, per-site \code{mean_N1},
and \code{converged}.
}
\description{
Fits the Dail-Madsen (2011) open-population N-mixture (Poisson initial
abundance, binomial survival, Poisson recruitment, binomial detection) by
maximising the exact HMM forward marginal likelihood with an analytic gradient
(BFGS). The observed-information covariance is the inverse of the negative
finite-difference Jacobian of the analytic gradient at the mode. All four arms
(initial abundance \code{lambda}, detection \code{p}, survival \code{omega}, recruitment
\code{gamma}) are site-level.
}
\references{
Dail, D., Madsen, L. (2011). Models for estimating abundance from
repeated counts of an open metapopulation. \emph{Biometrics} 67, 577-587.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{check_model.tobs_fit}
\alias{check_model.tobs_fit}
\title{Comprehensive model checking}
\usage{
\method{check_model}{tobs_fit}(object, coords = NULL, n.samples = 250, plot = TRUE, ...)
}
\arguments{
\item{object}{A \code{tobs_fit} object.}

\item{coords}{Optional \verb{n_sites x 2} coordinate matrix. Adds Moran's I on
the occupancy residuals and the correlogram panel.}

\item{n.samples}{Posterior samples for simulation tests.}

\item{plot}{Draw the panel. \code{FALSE} prints the report alone, for a log or a
headless run.}

\item{...}{Unused.}
}
\value{
Invisibly, the diagnostic results: \code{waic}, \code{dic}, \code{cpo}, \code{ppc},
\code{zero_inflation}, \code{dispersion}, \code{pit}, \code{uniformity}, and \code{moran} when
\code{coords} is supplied.
}
\description{
The \code{\link[tulpa:check_model]{tulpa::check_model()}} method for \code{tobs_fit}: the criteria, the
posterior-predictive check and the goodness-of-fit tests in one call,
printed as a report and drawn as the diagnostic panel.
}
\details{
The engine's default method builds its panel from \code{fitted()} and
\code{residuals()} as numeric vectors, which a latent-state fit does not have --
\code{fitted()} returns \code{list(psi, p, z)} and \code{residuals()} one series per
process, so the response cannot be resolved. This method reads the same
quantities through the family's own doors instead: \code{\link[=pit_residuals]{pit_residuals()}},
\code{\link[=ppc]{ppc()}}, \code{\link[=test_dispersion]{test_dispersion()}} and \code{\link[tulpa:moran_i]{tulpa::moran_i()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/removal.R
\name{removal_laplace}
\alias{removal_laplace}
\title{Laplace fit of the removal-sampling abundance model}
\usage{
removal_laplace(
  y,
  site_idx,
  X_lambda,
  X_p,
  mixture = c("P", "NB"),
  beta_lambda_init = NULL,
  beta_p_init = NULL,
  log_r_init = NULL,
  r_max = 1e+05,
  K_max = NULL,
  max_iter = 100L,
  tol = 1e-06,
  verbose = FALSE
)
}
\arguments{
\item{y}{Integer vector of per-pass removals (long form), passes in order.}

\item{site_idx}{Integer vector, same length as \code{y}, 1-based site index; a
site's passes must appear in pass order (the depletion order).}

\item{X_lambda}{Numeric matrix \verb{[n_sites x p_lambda]} of abundance covariates.}

\item{X_p}{Numeric matrix \verb{[n_obs x p_p]} of per-pass detection covariates.}

\item{mixture}{\code{"P"} (Poisson, default) or \code{"NB"} (negative binomial).}

\item{beta_lambda_init, beta_p_init, log_r_init}{Optional warm starts.}

\item{r_max}{Upper bound on the NB size \code{r} (NB only, default \code{1e5}).}

\item{K_max}{Marginal-sum truncation; defaults to \verb{max(site removal total) + 100}. Must be \code{>=} the largest per-site removal total.}

\item{max_iter}{Newton iteration budget (default 100).}

\item{tol}{Gradient-norm convergence tolerance (default 1e-6).}

\item{verbose}{Print per-iteration progress.}
}
\value{
A list of class \code{nmix_fit} (the shared count-marginal fit shape):
\code{beta_lambda}, \code{beta_p}, \code{mixture}, \code{log_r} / \code{r} (NB), \code{log_lik}, \code{vcov},
\code{H_obs}, \code{n_iter}, \code{converged}, \code{grad_norm}, per-site \code{mean_N} / \code{var_N} /
\code{boundary_weight}.
}
\description{
Maximum-likelihood fit (non-spatial, fixed effects only) of the removal /
sequential-depletion abundance model with a Poisson or negative-binomial
abundance mixing distribution. Pass \code{k} removes
\verb{y_\{ik\} ~ Binomial(N_i - sum_\{l<k\} y_\{il\}, p_\{ik\})} of the individuals still
present, and the latent \code{N_i ~ Poisson(lambda_i)} (or \code{NegBin(lambda_i, r)})
is summed out in closed form (truncation \code{K_max}). Optimisation is the same
analytic inner-Newton / observed-Fisher path the N-mixture uses; the per-site
detection arm sees the depleting available count rather than the full \code{N}.
}
\references{
Royle, J. A. (2004). N-mixture models for estimating population size from
spatially replicated counts. \emph{Biometrics} 60, 108-115.
Dorazio, R. M., Jelks, H. L., Jordan, F. (2005). Improving removal-based
estimates of abundance. \emph{Biometrics} 61, 1093-1101.
}

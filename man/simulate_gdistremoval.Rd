% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdistremoval.R
\name{simulate_gdistremoval}
\alias{simulate_gdistremoval}
\title{Simulate a joint distance + removal data set}
\usage{
simulate_gdistremoval(
  N = 200,
  cutpoints = c(0, 10, 20, 30, 40),
  n_periods = 4L,
  transect = "line",
  n_abund_covs = 1,
  n_det_covs = 1,
  n_rem_covs = 1,
  beta_lambda = NULL,
  beta_sigma = NULL,
  beta_r = NULL,
  seed = NULL
)
}
\arguments{
\item{N}{Number of sites (default 200).}

\item{cutpoints}{Distance-bin edges \verb{0 = c_0 < ... < c_B}.}

\item{n_periods}{Number of removal periods.}

\item{transect}{\code{"line"} (default) or \code{"point"}.}

\item{n_abund_covs, n_det_covs, n_rem_covs}{Number of abundance / distance /
removal covariates.}

\item{beta_lambda, beta_sigma, beta_r}{Coefficients on the log-abundance,
log-scale, and logit-removal arms (\code{c(intercept, slopes...)}). Defaults give
moderate abundance / detection.}

\item{seed}{Optional random seed.}
}
\value{
A list with \code{y} (distance-band counts), \code{y_rem} (removal-period
counts), \code{data}, and \code{truth}.
}
\description{
Draws from the \code{\link[=gdistremoval]{gdistremoval()}} model: site abundance \code{N ~ Poisson(lambda)},
the detected birds cross-classified by a distance band (half-normal key, scale
\code{sigma}) and by a removal period (per-period capture \code{r}).
}

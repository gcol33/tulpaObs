% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmix_laplace_spatial.R
\name{nmix_laplace_icar}
\alias{nmix_laplace_icar}
\title{Spatial Royle (2004) N-mixture model via nested Laplace}
\usage{
nmix_laplace_icar(
  y,
  site_idx,
  map_site_to_unit,
  X_lambda,
  X_p,
  adj_row_ptr,
  adj_col_idx,
  n_neighbors,
  n_spatial,
  tau_grid = NULL,
  mixture = c("P", "NB"),
  r_grid = NULL,
  beta_lambda_init = NULL,
  beta_p_init = NULL,
  z_init = NULL,
  K_max = NULL,
  max_iter = 100L,
  tol = 1e-06,
  verbose = FALSE
)
}
\arguments{
\item{y}{Integer vector of observed counts (long form, one entry per visit).}

\item{site_idx}{Integer vector, 1-based site index for each visit, same
length as \code{y}.}

\item{map_site_to_unit}{Integer vector of length \code{n_sites}, 1-based spatial
unit index for each site. Sites can share spatial units; the data
contribution to \code{z[u]} aggregates across all sites that map to \code{u}.}

\item{X_lambda}{Numeric matrix \verb{[n_sites x p_lambda]} of abundance covariates.}

\item{X_p}{Numeric matrix \verb{[n_obs x p_p]} of detection covariates.}

\item{adj_row_ptr, adj_col_idx, n_neighbors}{CSR adjacency for the ICAR graph
on the \code{n_spatial} units. \code{adj_row_ptr} has length \code{n_spatial + 1},
\code{adj_col_idx} lists 0-based neighbours, and \code{n_neighbors[s]} is the row
degree of unit s.}

\item{n_spatial}{Number of spatial units.}

\item{tau_grid}{Optional numeric vector of \eqn{\tau} grid points. Defaults
to \code{exp(seq(log(0.3), log(30), length.out = 9))}.}

\item{mixture}{Abundance mixing distribution: \code{"P"} (Poisson, default) or
\code{"NB"} (negative binomial). Under \code{"NB"} the NB size \eqn{r} is integrated
as an additional outer grid dimension (alongside \eqn{\tau}); the posterior
\code{r_mean} / \code{r_sd} are reported from the grid weights.}

\item{r_grid}{Optional numeric vector of NB size grid points (NB only).
Defaults to \code{exp(seq(log(0.5), log(40), length.out = 6))}. Ignored under
Poisson.}

\item{beta_lambda_init}{Optional warm start; default \code{c(log(mean(y)+0.1), 0, ...)}.}

\item{beta_p_init}{Optional warm start; default \code{rep(0, p_p)}.}

\item{z_init}{Optional warm start for the spatial field; default zeros.}

\item{K_max}{Truncation for the per-site marginal sum over N. Defaults to
\code{max(y) + 100}. Returned \code{boundary_max} flags any grid point whose worst
site puts non-trivial mass on \code{K_max} -- raise \code{K_max} if it exceeds 1e-4.}

\item{max_iter, tol}{Inner Newton iteration budget and gradient-norm tol.}

\item{verbose}{Print per-iteration and per-grid-point progress.}
}
\value{
A list of class \code{nmix_spatial_fit}:
\itemize{
\item \code{tau_grid} -- input grid
\item \code{log_marginal} -- log marginal at each tau (up to a tau-independent constant)
\item \code{weights} -- normalised grid weights (sum to 1)
\item \code{tau_mean}, \code{tau_sd} -- posterior moments of tau
\item \code{modes} -- \verb{[n_grid x (p_lambda + p_p + n_spatial)]} matrix of inner modes
\item \code{beta_lambda_mean}, \code{beta_p_mean} -- weighted-mean coefficient estimates
\item \code{z_mean} -- weighted-mean spatial field
\item \code{n_iter}, \code{converged}, \code{grad_norm}, \code{log_lik}, \code{boundary_max} -- per-grid diagnostics
\item \code{p_lambda}, \code{p_p}, \code{n_spatial}, \code{K_max} -- echoed dimensions
\item \code{call} -- matched call
}
}
\description{
Nested-Laplace fit of the spatial N-mixture model
\deqn{N_i \sim \mathrm{Poisson}(\lambda_i), \qquad
      y_{ij} | N_i \sim \mathrm{Binomial}(N_i, p_{ij}),}
with abundance linear predictor
\eqn{\log \lambda_i = X_\lambda^{(i)} \beta_\lambda + z_{u(i)}} where
\eqn{z \sim \mathrm{ICAR}(\tau)} is an intrinsic conditional autoregressive
field on the user-supplied adjacency graph, and detection linear predictor
\eqn{\mathrm{logit}\, p_{ij} = X_p^{(ij)} \beta_p}.

The hyperparameter \eqn{\tau} (ICAR precision) is integrated out by an
outer grid: at each \eqn{\tau_k} the inner Newton finds the joint mode
of \eqn{(\beta_\lambda, \beta_p, z)} and the Laplace log-marginal
\eqn{\log p(y \mid \tau_k)} is accumulated. Posterior weights over
\eqn{\tau} normalise the grid.

Inner Newton uses the marginal observed Fisher information matrix for
curvature (with the Var\eqn{[N \mid y_i]} rank-1 correction encoding
cross-arm coupling) and falls back to the complete-data Fisher block when
the observed-info matrix is not PSD. A small diagonal ridge keeps the
Cholesky stable through the (intercept, constant-\eqn{z}) structural null
direction; \eqn{z} is centered to sum zero after every step.
}
\references{
Royle, J. A. (2004). N-mixture models for estimating population size from
spatially replicated counts. \emph{Biometrics} 60, 108-115.
Besag, J., York, J., Mollie, A. (1991). Bayesian image restoration with two
applications in spatial statistics. \emph{Ann. Inst. Statist. Math.} 43, 1-20.
Rue, H., Martino, S., Chopin, N. (2009). Approximate Bayesian inference for
latent Gaussian models by using integrated nested Laplace approximations.
\emph{JRSS-B} 71, 319-392.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obs_families.R
\name{occu_multiscale_cover}
\alias{occu_multiscale_cover}
\title{Three-level (multiscale) occupancy + cover hurdle family}
\usage{
occu_multiscale_cover(response = c("beta", "lognormal", "gaussian"))
}
\arguments{
\item{response}{likelihood for the positive cover arm. \code{"beta"} (cover in
(0, 1)), \code{"lognormal"} (log-cover Gaussian), or \code{"gaussian"} (an
identity-link Gaussian magnitude, the delta-normal hurdle; for a
pre-transformed / unbounded positive response, not raw cover fractions).}
}
\value{
A \code{tobs_family} object.
}
\description{
A cell-level occupancy gate, a plot-level availability gate, per-visit
detection, and the cover hurdle, for data where a site's "visits" are
spatially distinct plots aggregated into a \verb{(cell, period)} rather than
temporal revisits (the EVA / MOTIVATE vegetation layout; Nichols et al.
2008; Mordecai et al. 2011). \code{\link[=occu_cover]{occu_cover()}} treats plots as detection
replicates of one occupancy state, which on spatial subunits conflates
within-cell prevalence into the detection arm (Kendall & White 2009); this
family separates them with an explicit middle level:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{z_c        ~ Bernoulli(psi_c)                 # cell / range occupancy
a_cj | z=1 ~ Bernoulli(theta_cj)              # plot availability / use
y_cjv|a=1  ~ Bernoulli(p_cjv)                 # detection
cover|y=1  ~ f_pos(.; eta_pos, disp)          # cover hurdle (beta / lognormal)
}\if{html}{\out{</div>}}

Both \code{z} (over cells) and \code{a} (over plots) marginalize in closed form (two
states each), so the joint marginal log-likelihood is exact and reuses the
same nested-Laplace cell-coupling machinery as \code{\link[=occu_cover]{occu_cover()}}.
}
\section{Inputs}{

\code{y} / \code{y_pos} are \verb{[n_plots x max_visits]} matrices (plots are the rows, the
availability units; visits the columns). The state-process \code{formula} is the
cell-level occupancy predictor and MUST carry an areal field naming the
per-plot cell column, \code{icar(graph = adj, group_var = "cell")}. \code{availability = ~ ...} is the plot-level theta predictor (default \code{~ 1}); \code{detection} the
per-visit p predictor; \code{positive = ~ ...} the cover predictor (default the
detection formula). \code{y_pos} is read only where \code{y == 1}.

A detected visit (\code{y_cjv = 1}) with a missing cover (\code{y_pos_cjv = NA}) keeps
its detection term but drops the \code{f_pos} factor: cover is taken
missing-at-random, so the cover likelihood runs over the detected visits with
an observed cover. This is the rule \code{\link[=occu_cover]{occu_cover()}} applies, and it holds on
every engine below.
}

\section{Identifiability}{

The availability (\code{theta}) and detection (\code{p}) levels separate only with
replication WITHIN a plot. Single releves supply none, so a plain fit
identifies \code{psi} (cell) and the product \code{theta * p} (plot) -- it reduces to
\code{\link[=occu_cover]{occu_cover()}} with \code{p := theta * p}. Within-plot temporal replication (e.g.
a resurvey of the same plot in a later period) makes the third level
estimable.
}

\section{Scope}{

Three engines. \code{method = "nested_laplace"} carries a single shared areal
field coupled across the occupancy (\code{sigma}) and cover (\code{alpha * sigma})
arms, integrated over the outer \verb{(sigma, alpha)} grid. \code{method = "laplace"}
and \code{method = "nuts"} are the non-spatial path (iid cells, no field): the
exact three-level marginal (z over cells, a over plots both summed in closed
form) optimised directly (\code{"laplace"}, a Gaussian observed-Fisher posterior)
or sampled (\code{"nuts"}, the exact coefficient posterior with calibrated
intervals and WAIC / LOO). Cells are declared the same way on every path, via
an \code{icar(graph = adj, group_var = "<cell>")} term (the graph drives the field
under \code{"nested_laplace"} and supplies only the plot -> cell map under
\code{"laplace"} / \code{"nuts"}). On the \code{"nested_laplace"} path additional weighted
areal terms in the psi formula
(\verb{icar(graph = adj, group_var = "<cell>", weight = <cell covariate>)}) add
spatially-varying-coefficient trend fields, each coupled onto the cover arm
with its own \code{alpha_trend}; the fitted fields are in \code{fit$trend_field} /
\code{fit$trend_fields}. The coupled / trend field is not sampled, so
\code{method = "nuts"} takes a single cell-declaring areal term only.

The coupling is written as \code{copy(spatial())} in the \code{positive} formula, the
same spelling \code{\link[=occu_cover]{occu_cover()}} takes; it needs a field to copy, so it is
accepted under \code{method = "nested_laplace"} and refused on the two
non-spatial engines, which fix the field at 0. Omitting it leaves the
amplitude on the engine's default axis, estimated -- unlike
\code{\link[=occu_cover]{occu_cover()}}, where a missing \code{copy()} pins \code{alpha = 0}.

The copy amplitude's axis carries prior structure -- a point mass at
\code{alpha = 0} and a log-spaced slab above it -- so \code{copy(alpha = grid(...))}
STATES its nodes, while \code{copy(alpha = grid(n = ))} states a RESOLUTION: the
engine re-reads its own axis with that many slab nodes, point mass and bounds
unchanged. \verb{terms =} gives either, per block, and
\code{control$alpha.grid[.trend]} / \code{control$alpha.n[.trend]} are the lower-level
spelling of the same pair. One block takes one of the two.
}

\seealso{
\code{\link[=occu_cover]{occu_cover()}} (two-level), \code{\link[=cover]{cover()}} (plot hurdle, no detection).
}

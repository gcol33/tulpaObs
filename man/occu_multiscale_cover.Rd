% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obs_families.R
\name{occu_multiscale_cover}
\alias{occu_multiscale_cover}
\title{Three-level (multiscale) occupancy + cover hurdle family}
\usage{
occu_multiscale_cover(positive = c("beta", "lognormal"))
}
\arguments{
\item{positive}{likelihood for the positive cover arm. \code{"beta"} (cover in
(0, 1)) or \code{"lognormal"} (log-cover Gaussian).}
}
\value{
A \code{tobs_family} object.
}
\description{
A cell-level occupancy gate, a plot-level availability gate, per-visit
detection, and the cover hurdle, for data where a site's "visits" are
spatially distinct plots aggregated into a \verb{(cell, period)} rather than
temporal revisits (the EVA / MOTIVATE vegetation layout; Nichols et al.
2008; Mordecai et al. 2011). \code{\link[=occu_cover]{occu_cover()}} treats plots as detection
replicates of one occupancy state, which on spatial subunits conflates
within-cell prevalence into the detection arm (Kendall & White 2009); this
family separates them with an explicit middle level:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{z_c        ~ Bernoulli(psi_c)                 # cell / range occupancy
a_cj | z=1 ~ Bernoulli(theta_cj)              # plot availability / use
y_cjv|a=1  ~ Bernoulli(p_cjv)                 # detection
cover|y=1  ~ f_pos(.; eta_pos, disp)          # cover hurdle (beta / lognormal)
}\if{html}{\out{</div>}}

Both \code{z} (over cells) and \code{a} (over plots) marginalize in closed form (two
states each), so the joint marginal log-likelihood is exact and reuses the
same nested-Laplace cell-coupling machinery as \code{\link[=occu_cover]{occu_cover()}}.
}
\section{Inputs}{

\code{y} / \code{y_pos} are \verb{[n_plots x max_visits]} matrices (plots are the rows, the
availability units; visits the columns). The state-process \code{formula} is the
cell-level occupancy predictor and MUST carry an areal field naming the
per-plot cell column, \code{icar(graph = adj, group_var = "cell")}. \code{availability = ~ ...} is the plot-level theta predictor (default \code{~ 1}); \code{detection} the
per-visit p predictor; \code{positive = ~ ...} the cover predictor (default the
detection formula). \code{y_pos} is read only where \code{y == 1}.
}

\section{Identifiability}{

The availability (\code{theta}) and detection (\code{p}) levels separate only with
replication WITHIN a plot. Single releves supply none, so a plain fit
identifies \code{psi} (cell) and the product \code{theta * p} (plot) -- it reduces to
\code{\link[=occu_cover]{occu_cover()}} with \code{p := theta * p}. Within-plot temporal replication (e.g.
a resurvey of the same plot in a later period) makes the third level
estimable.
}

\section{Scope (status \code{"experimental"})}{

Two engines. \code{method = "nested_laplace"} carries a single shared areal field
coupled across the occupancy (\code{sigma}) and cover (\code{alpha * sigma}) arms,
integrated over the outer \verb{(sigma, alpha)} grid. \code{method = "laplace"} is the
non-spatial path (iid cells, no field): the exact three-level marginal
optimised directly. Cells are declared the same way on both paths, via an
\code{icar(graph = adj, group_var = "<cell>")} term (the graph drives the field
under \code{"nested_laplace"} and is ignored under \code{"laplace"}). Spatially varying
trend fields are not yet wired.
}

\seealso{
\code{\link[=occu_cover]{occu_cover()}} (two-level), \code{\link[=cover]{cover()}} (plot hurdle, no detection).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obs_families.R
\name{occu_multi}
\alias{occu_multi}
\title{Multi-species co-occurrence occupancy family}
\usage{
occu_multi()
}
\value{
A \code{tobs_family} object for \code{\link[=tobs]{tobs()}}.
}
\description{
Joint occupancy of \code{S} species where the occupancy state \verb{z in \{0,1\}^S}
follows a log-linear model with first-order (per species) and second-order
(per species pair) natural parameters (Rota et al. 2016; \pkg{unmarked}
\code{occuMulti}). The second-order parameters are the species interactions
(positive = co-occur more than independent; negative = avoid). The state
\code{formula} is the shared occupancy covariate design (each natural parameter
carries its own coefficients); \code{detection} the shared site-level per-species
detection design. The latent state integrates out by enumerating the \code{2^S}
states, so the exact marginal is maximised with an observed-information vcov.
}
\details{
\code{y} is a length-\code{S} list of \verb{N x J} 0/1/NA detection matrices (or a 3D
\verb{[sites x visits x species]} array); \code{species} names the arms.
}
\examples{
\donttest{
sim <- simulate_occu_multi(S = 2, N = 300, seed = 1)
fit <- tobs(~ scov1, data = sim$data, family = occu_multi(),
            detection = ~ 1, y = sim$y, species = sim$species,
            control = list(verbose = FALSE))
coef(fit)
}
}

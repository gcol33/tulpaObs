% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obs_families.R
\name{ms_distance}
\alias{ms_distance}
\title{Community (multispecies) binned distance-sampling family}
\usage{
ms_distance(
  key = c("halfnorm", "hazard"),
  transect = c("line", "point"),
  cutpoints = NULL,
  K_max = NULL,
  mixture = c("poisson", "negbin")
)
}
\arguments{
\item{key}{Detection function: \code{"halfnorm"} (default) or \code{"hazard"}. Under the
hazard-rate key the scalar log-shape is shared across species.}

\item{transect}{Transect geometry: \code{"line"} (default) or \code{"point"}.}

\item{cutpoints}{Distance-bin edges, length \code{dim(y)[2] + 1}, strictly
increasing and starting at \verb{>= 0}.}

\item{K_max}{Truncation for the latent abundance sum. Defaults to
\code{3 * max(rowSums(y)) + 100}.}

\item{mixture}{Abundance mixing distribution. \code{"poisson"} only; the
negative-binomial size is not yet carried as a per-species random effect.}
}
\value{
A \code{tobs_family} object.
}
\description{
The community analogue of \code{\link[=distance]{distance()}}: per-species binned distance sampling
with Gaussian community hyperpriors on the per-species abundance and
detection-scale coefficients, so rare species borrow strength from common ones
through the shared community means and covariances -- the same pooling that
stabilises \code{\link[=ms_abun]{ms_abun()}}, on the distance-sampling response.
}
\details{
Per species \code{s}, site \code{i}, distance bin \code{b}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{N_\{s,i\}        ~ Poisson(lambda_\{s,i\})
y_\{s,i,.\} | N  ~ Multinomial(N_\{s,i\}; pi_\{s,i,1..B\}, 1 - p_det)
log lambda_\{s,i\} = X_i     . (mu_lambda + b_lambda_s)
log sigma_\{s,i\}  = X_sig_i . (mu_sigma  + b_sigma_s)
b_lambda_s ~ N(0, Sigma_lambda),  b_sigma_s ~ N(0, Sigma_sigma)
}\if{html}{\out{</div>}}

with \code{pi_b} the integral of the detection function over bin \code{b}. The latent
\verb{N_\{s,i\}} integrates out per species-site in closed form, exactly as for
\code{\link[=distance]{distance()}}; the per-species coefficient deviations are the random effects,
integrated by the shared community Laplace-EM.

Adding \code{latent(n)} to the abundance formula gives residual species
co-occurrence through \code{n} per-site latent factors with per-species loadings;
adding an areal term (\code{icar()} / \code{car_proper()} / \code{bym2()}) or \code{spde()}
alongside gives a shared spatial field. Both route through the same
block-coordinate engine as the other community families.
}
\section{Inputs}{

\code{y} is a 3D array \verb{[n_sites x n_bins x n_species]} or a named list of
\verb{n_sites x n_bins} per-bin count matrices. \code{formula} is the abundance
(\verb{log lambda}) predictor, \code{detection} the detection-scale (\verb{log sigma})
predictor, and \code{species} the species labels.
}

\seealso{
\code{\link[=distance]{distance()}} (single species), \code{\link[=ms_abun]{ms_abun()}} (community N-mixture).
}

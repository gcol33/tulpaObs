% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family_cover_hurdle.R
\name{fit_cover_hurdle}
\alias{fit_cover_hurdle}
\title{Fit the two arms of a cover hurdle}
\usage{
fit_cover_hurdle(
  enc,
  positive = enc$positive,
  engine = "laplace",
  priors = NULL,
  control = list()
)
}
\arguments{
\item{enc}{Output of \code{\link[=encode_cover_hurdle]{encode_cover_hurdle()}}.}

\item{positive}{\code{"lognormal"} or \code{"beta"} (taken from \code{enc$positive}).}

\item{engine}{\code{"laplace"} (default) or \code{"nested_laplace"}. The latter is
routed through \code{\link[=fit_cover_hurdle_joint_nested]{fit_cover_hurdle_joint_nested()}}.}

\item{priors}{Optional \code{\link[=cover_priors]{cover_priors()}} object (or a coercible list /
\code{FALSE}). Adds a weakly-informative fixed-effect penalty on both arms
(occurrence + positive, beta or lognormal); \code{NULL} / \code{FALSE} fit
unpenalised. Rejected with a spatial formula (the spatial solver carries
its own prior).}

\item{control}{List with optional \code{max.iter}, \code{tol}, \code{n.threads}.}
}
\value{
List with \code{m_occ}, \code{m_pos}, \code{positive}, \code{pos_fit_n}, \code{pos_fit_p},
plus one of \code{sigma_pos} (lognormal) or \code{phi_pos} (beta).
}
\description{
Two independent \code{tulpa::tulpa_laplace()} calls. The joint shared-field
fit is Phase 1c. For \code{positive = "lognormal"} the positive arm is a
Gaussian fit on \code{log(cover)} with sigma estimated post-hoc as the
residual standard error. For \code{positive = "beta"} the positive arm uses
\code{tulpa::tulpa_laplace_beta()} which estimates the precision \code{phi} via
an outer 1-D optimisation and weights the Hessian accordingly.
}
\keyword{internal}

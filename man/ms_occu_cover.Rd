% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obs_families.R
\name{ms_occu_cover}
\alias{ms_occu_cover}
\title{Community (multispecies) joint occupancy-detection + cover family}
\usage{
ms_occu_cover(positive = c("beta", "lognormal"))
}
\arguments{
\item{positive}{likelihood for the positive cover arm. \code{"beta"} (cover in
(0, 1)) or \code{"lognormal"} (log-cover Gaussian).}
}
\value{
A \code{tobs_family} object.
}
\description{
The community version of \code{\link[=occu_cover]{occu_cover()}}: a per-species joint occupancy-cover
model with Gaussian community hyperpriors on the per-species coefficients of
all three arms (occupancy \code{psi}, detection \code{p}, and positive cover). Rare
species borrow strength from common ones through the shared community means
and covariances, the same pooling that stabilises \code{\link[=ms_occu]{ms_occu()}} and
\code{\link[=ms_abun]{ms_abun()}} but on the joint occupancy + vegetation-cover response.
}
\details{
Per species \code{s}, cell \code{i}, visit \code{j}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{z_\{s,i\}        ~ Bernoulli(psi_\{s,i\})
y_\{s,i,j\} | z  ~ Bernoulli(p_\{s,i,j\})
c_\{s,i,j\} | y  ~ f_pos(eta_pos_\{s,i,j\}, disp)
logit psi_\{s,i\}  = X_occ_i . (mu_occ + b_occ_s)
logit p_\{s,i,j\}  = X_p_\{ij\} . (mu_p   + b_p_s)
g(cover)         = X_pos_\{ij\} . (mu_pos + b_pos_s)
b_occ_s ~ N(0, Sigma_occ), b_p_s ~ N(0, Sigma_p),
b_pos_s ~ N(0, Sigma_pos)
}\if{html}{\out{</div>}}

The latent presence \code{z} integrates out per species-cell in closed form (the
same two-state mixture as \code{\link[=occu_cover]{occu_cover()}}); the per-species coefficient
deviations are the random effects, integrated by a Laplace-EM. The
positive-arm dispersion is a shared community parameter.
}
\section{Inputs}{

\code{y} and \code{y_pos} are 3D arrays \verb{[n_sites x max_visits x n_species]} (or named
lists of \verb{n_sites x max_visits} matrices, one per species); \verb{species =} is
required. The occupancy \code{formula}, the \code{detection} formula, and the cover
\code{positive} formula carry community covariates shared across species. \code{coef()}
returns the community means; \code{ranef()} the per-species coefficient deviations.
}

\section{Reduced-rank spatial factors}{

A single areal field term (\code{icar(graph = adj)}, \code{car_proper(graph = adj)}, or
\code{bym2(graph = adj)}) on the occupancy \code{formula} fits the reduced-rank (HMSC /
spatial-gllvm) community model: \code{K} shared latent fields with per-species
loadings on the occupancy predictor,
\verb{logit psi_sc = X_c (mu_occ + b_occ_s) + sum_k L_sk w_kc}. A single shared
field with only a species intercept can shift each map's level but not its
shape; the loadings give each species its own spatial shape as a combination
of the shared factors, borrowing strength so rare taxa get a calibrated map.
The number of factors is set by \code{control = list(n.factors = K)}, or chosen
automatically by \code{control = list(n.factors = "auto", n.factors.max = M)},
which fits the identified (lower-triangular loading) ladder and picks the rank
that maximises the empirical-Bayes Laplace marginal likelihood (the field is
integrated out, so its prior supplies the Occam penalty -- latent-level
criteria such as WAIC track the field's effective dimension, not the rank, and
under-select). The per-rank evidence is returned in \code{fit$spatial$K_selection}.
\code{sd.load} sets the loading prior scale.

Adding the SAME \code{icar(graph = adj)} term to the cover \code{positive} formula
shares the latent fields across the two processes: the fields then also load on
the cover predictor through a free loading matrix,
\verb{g(cover)_sc = X_c (mu_pos + b_pos_s) + sum_k Lpos_sk w_kc}, so a species'
spatial occupancy pattern and its spatial cover pattern are linked through one
set of factors. The cover loadings are returned in \code{fit$spatial$loadings_cover}.
The field is shared, so the cover-arm term must match the occupancy arm (same
type and graph), and a cover-arm field without a matching occupancy field errors.

The field structure is set by the term: \code{icar()} (improper intrinsic CAR, the
default), \code{car_proper(graph = adj)} (a proper CAR whose per-factor correlation
\code{rho} is returned in \code{fit$spatial$rho_w}), or \code{bym2(graph = adj)} (the
Riebler 2016 convolution whose per-factor spatial-variance fraction \code{phi} is
returned in \code{fit$spatial$phi_w}); the field hyperparameter is estimated by EM.
All three share one engine; \code{fit$spatial$field_type} records the choice.
Structured terms on the detection arm, or an unsupported field term, error from
the dispatcher.
}

\section{Scope (status \code{"experimental"})}{

The non-spatial fit and the occupancy-arm reduced-rank spatial fit are Laplace
/ Laplace-EM. A shared coupled spatial field across all three arms with the
per-species RE block layered on top (the community analogue of
\code{\link[=occu_cover]{occu_cover()}}'s \code{nested_laplace} joint-coupled engine) needs upstream engine
support that does not yet combine a per-group RE block with a shared latent
field on one predictor; such a structured term on any arm errors from the
dispatcher rather than being silently dropped.
}

\seealso{
\code{\link[=occu_cover]{occu_cover()}} (single species), \code{\link[=ms_occu]{ms_occu()}} (community occupancy,
no cover), \code{\link[=ms_abun]{ms_abun()}} (community N-mixture).
}

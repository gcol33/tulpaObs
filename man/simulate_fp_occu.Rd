% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fp_occu.R
\name{simulate_fp_occu}
\alias{simulate_fp_occu}
\title{Simulate multistate false-positive occupancy data}
\usage{
simulate_fp_occu(
  N = 300,
  J = 5,
  n_occ_covs = 1,
  beta_psi = NULL,
  p11 = 0.6,
  p10 = 0.05,
  b = 0.5,
  seed = NULL
)
}
\arguments{
\item{N}{Number of sites (default 300).}

\item{J}{Number of visits (default 5).}

\item{n_occ_covs}{Number of occupancy covariates (default 1).}

\item{beta_psi}{Occupancy coefficients (logit). Default
\code{c(qlogis(0.5), runif(n_occ_covs, -0.6, 0.6))}.}

\item{p11, p10, b}{True detection, false-positive, and certain-classification
probabilities (scalars; defaults 0.6, 0.05, 0.5).}

\item{seed}{Optional random seed.}
}
\value{
A list with \code{y} (N x J state matrix), \code{data} (occupancy covariates),
and \code{truth} (coefficients, per-site \code{psi}, scalar \code{p11}/\code{p10}/\code{b}, latent \code{z}).
}
\description{
Latent occupancy \code{z_i ~ Bernoulli(psi_i)} with \verb{logit psi = X_psi beta_psi},
observed through \code{J} replicate visits emitting a detection state \verb{y in \{0,1,2\}}:
at an occupied site a visit detects with probability \code{p11} and the detection is
certain (state 2) with probability \code{b} else ambiguous (state 1); at an
unoccupied site a visit yields a false-positive ambiguous detection (state 1)
with probability \code{p10}. Returns an \verb{N x J} integer matrix in \verb{\{0, 1, 2\}}
suitable for \code{\link[=tobs]{tobs()}} with \code{\link[=fp_occu]{fp_occu()}}.
}

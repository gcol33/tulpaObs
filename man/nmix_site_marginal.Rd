% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmix_site_marginal.R
\name{nmix_site_marginal}
\alias{nmix_site_marginal}
\title{Per-site N-mixture marginal as a composable random-effect callback}
\usage{
nmix_site_marginal(
  y,
  site_idx,
  X_lambda,
  X_p,
  mixture = c("P", "NB"),
  K_max = NULL
)
}
\arguments{
\item{y}{Integer vector of observed counts, one entry per visit (long form).}

\item{site_idx}{Integer vector, same length as \code{y}, 1-based site index
indicating which site each visit belongs to.}

\item{X_lambda}{Numeric matrix \verb{[n_sites x p_lambda]} of abundance covariates.}

\item{X_p}{Numeric matrix \verb{[n_obs x p_p]} of detection covariates (long form,
row order matches \code{y}).}

\item{mixture}{Abundance mixing distribution: \code{"P"} (Poisson, default) or
\code{"NB"} (negative binomial). Under \code{"NB"} an extra dispersion parameter
\code{log_r} (log NB size) is estimated jointly and reported with its standard
error.}

\item{K_max}{Marginal-sum truncation. Defaults to \code{max(y) + 100} (matches
\code{unmarked::pcount}). The returned \code{boundary_weight} per site flags any
site whose posterior over N puts non-trivial mass on \code{K_max}; raise
\code{K_max} if any such weight exceeds ~1e-4.}
}
\value{
An object of class \code{nmix_marginal}: a list of the validated
data plus closures
\itemize{
\item \code{eval(eta_lambda, eta_p, r = Inf)} -- evaluate at the abundance log
predictor \code{eta_lambda} (length \code{n_sites}) and detection logit predictor
\code{eta_p} (length \code{n_obs}, visit order matching \code{y}). Returns a list with
\code{log_lik} (total), \code{log_lik_site}, \code{grad_eta_lambda}, \code{grad_eta_p},
\code{grad_theta}, the complete-data Fisher \code{info_eta_lambda} / \code{info_eta_p},
the abundance score weight \code{score_wt_lambda}, \code{p} (= \code{plogis(eta_p)}),
\code{mean_N}, \code{var_N}, \code{boundary_weight}, and (NB) the dispersion-coupling
pieces \code{info_theta} / \code{info_lambda_theta} / \code{cov_N_stheta} / \code{var_stheta}.
\item \code{eval_beta(beta_lambda, beta_p, r = Inf)} -- the same, with the predictors
formed from the stored design matrices.
\item \code{obs_info_block(s, ev)} -- the \eqn{(1+J_s)\times(1+J_s)} per-site
marginal observed-information matrix \eqn{B_s} (above) for site \code{s}, from
an \code{eval()} / \code{eval_beta()} result \code{ev}. Coordinates are
\verb{(eta_lambda_s, eta_p over site s's visits in input order)}.
Plus \code{n_sites}, \code{n_obs}, \code{p_lambda}, \code{p_p}, \code{mixture}, \code{K_max},
\code{obs_by_site} (per-site visit row indices).
}
}
\description{
Exposes the Royle (2004) N-mixture per-site marginal -- the latent abundance
\eqn{N_i} summed out in closed form -- as a reusable building block for
integrating grouped random effects over the abundance and/or detection
linear predictors. It is the bridge between the marginal kernel (which
already differentiates through both arms) and a random-effect integrator
such as \code{\link[tulpa:tulpa_re_aghq]{tulpa_re_aghq()}}.

Unlike \code{\link[=nmix_laplace]{nmix_laplace()}}, which fixes the coefficients and returns a
point fit, this helper returns a \emph{closure object} that evaluates the marginal
and its eta-level derivatives at arbitrary linear predictors. A random-effect
integrator perturbs the predictors by \eqn{Z b} per group and calls \code{eval()}
at each quadrature point; the per-site value, gradient and observed-
information block are everything the per-group integral needs.

The abundance arm is per site (\eqn{\log\lambda_i = \eta^\lambda_i}) and the
detection arm is per visit (\eqn{\mathrm{logit}\,p_{ij} = \eta^p_{ij}}); the
two are coupled through the shared latent \eqn{N_i}. The per-site marginal
observed information in the eta coordinates
\eqn{(\eta^\lambda_i, \eta^p_{i1}, \dots, \eta^p_{iJ_i})} is
\deqn{B_i = \mathrm{diag}(I^\lambda_i, I^p_{ij}) - \mathrm{Var}(N_i\mid y_i)\,
      v_i v_i^\top, \qquad v_i = (-w_i, p_{i1}, \dots, p_{iJ_i}),}
where \eqn{I^\lambda_i}, \eqn{I^p_{ij}} are the complete-data Fisher diagonal,
\eqn{w_i} is the abundance score weight (\code{1} Poisson, \eqn{1-q_i} NB) and
\eqn{p_{ij} = \mathrm{plogis}(\eta^p_{ij})}. The off-diagonal
\eqn{\mathrm{Var}(N_i)\,w_i\,p_{ij}} is the abundance/detection coupling an
integrator placing random effects on both arms must carry (Louis 1982). This
is the eta-level form of the curvature \code{\link[=nmix_laplace]{nmix_laplace()}} sandwiches with
the design matrices.

The single- vs multi-arm \code{make_site} adapter that wires this into a specific
integrator is deliberately not built here -- this object is the arm-agnostic
foundation it sits on.
}
\references{
Royle, J. A. (2004). N-mixture models for estimating population size from
spatially replicated counts. \emph{Biometrics} 60, 108-115.
Louis, T. A. (1982). Finding the observed information matrix when using the
EM algorithm. \emph{JRSS-B} 44, 226-233.
}
\seealso{
\code{\link[=nmix_laplace]{nmix_laplace()}} for the fixed-effects fit, \code{\link[tulpa:tulpa_re_aghq]{tulpa_re_aghq()}}
for the grouped random-effect integrator this feeds.
}

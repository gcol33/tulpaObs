% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_cover_hurdle.R
\name{simulate_cover_joint}
\alias{simulate_cover_joint}
\title{Simulate cover-hurdle data with a shared BYM2 spatial field}
\usage{
simulate_cover_joint(
  N = 300L,
  adj,
  beta_occ = c(-0.3, 0.7),
  beta_pos = c(0.4, -0.5),
  sigma = 0.6,
  rho = 0.7,
  alpha = 1,
  positive = c("beta", "lognormal"),
  phi = 30,
  sigma_pos_resid = 0.4,
  seed = NULL
)
}
\arguments{
\item{N}{Number of sites (default 300).}

\item{adj}{\verb{n_s x n_s} integer adjacency matrix for the BYM2 field.}

\item{beta_occ}{Length-2 occurrence coefficients (intercept + slope).}

\item{beta_pos}{Length-2 cover-arm coefficients (intercept + slope).}

\item{sigma}{Marginal spatial-field amplitude (default 0.6); the
occurrence-arm linear predictor adds \code{sigma * z[region]}.}

\item{rho}{BYM2 mixing parameter, in \verb{[0, 1]} (default 0.7).
\code{rho = 1} is pure ICAR; \code{rho = 0} is pure IID.}

\item{alpha}{Cover-arm scaling: cover-arm linear predictor adds
\code{alpha * sigma * z[region]} (so \code{sigma_pos = alpha * sigma}).}

\item{positive}{Likelihood for the positive arm: \code{"beta"} or
\code{"lognormal"}.}

\item{phi}{Beta precision when \code{positive = "beta"} (default 30).}

\item{sigma_pos_resid}{Lognormal residual SD when \code{positive = "lognormal"} (default 0.4); independent of the spatial \code{sigma}.}

\item{seed}{Optional integer seed.}
}
\value{
A list with:
\describe{
\item{data}{Data frame with \code{x} and \code{region} (factor).}
\item{y}{Length-N numeric vector of cover values (zeros where
\code{occur = 0}).}
\item{adj}{The adjacency matrix passed in (for downstream
\code{tulpa::spatial_bym2()}).}
\item{truth}{Named list with \code{beta_occ}, \code{beta_pos}, \code{sigma},
\code{rho}, \code{alpha}, \code{sigma_pos = alpha * sigma}, \code{positive}, plus the
simulated \code{phi_f}, \code{theta_f}, \code{w_s}, \code{region}.}
}
}
\description{
Generates synthetic data matching the joint nested-Laplace cover-hurdle
parameterisation (\code{sigma_occ * z} on the occurrence arm, \code{alpha * sigma * z} on the cover arm) where \code{z} is a BYM2 latent field on the
supplied region adjacency. The cover-arm field amplitude
\code{sigma_pos = alpha * sigma} is the derived ratio surfaced as the
\code{"alpha"} column of \code{fit$joint$theta_grid} (\code{gcol33/tulpa#18}).
}
\details{
Both BYM2 sub-blocks (\code{phi}, \code{theta}) are drawn IID Normal \strong{and
demeaned to mean zero} before scaling, matching the sum-to-zero
constraint applied to both sub-blocks by \code{tulpa::spatial_bym2()} inside
the nested-Laplace engine (see \code{.joint_inner_var()}). Without the
demean each seed carries \code{mean(w_s) ~ N(0, sigma^2 / n_s)} and the
constrained intercept identified by the engine targets
\code{beta_pos_0_truth + alpha * mean(w_s)} rather than \code{beta_pos_0_truth};
coverage of the \emph{population} truth then collapses with alpha while
the engine-reported posterior is correctly calibrated for the
constrained parameter. See \code{example/validation/SUMMARY.md} in
INLAabun (Demo 3) for the diagnosis.
}
\examples{
adj <- matrix(0L, 10, 10)
for (s in 1:10) for (j in setdiff(c(s - 1L, s + 1L), c(0L, 11L)))
  adj[s, j] <- 1L
sim <- simulate_cover_joint(N = 100, adj = adj, alpha = 1.0, seed = 1)
head(sim$data)
}

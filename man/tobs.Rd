% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tobs.R
\name{tobs}
\alias{tobs}
\title{Fit a hierarchical latent-state observation model}
\usage{
tobs(
  formula,
  data,
  family,
  detection = NULL,
  y = NULL,
  visit_data = NULL,
  engine = c("auto", "laplace", "nested_laplace", "nuts"),
  approx = c("gaussian_laplace", "simplified_laplace"),
  priors = NULL,
  control = list(),
  ...
)
}
\arguments{
\item{formula}{state-process formula, e.g. \code{~ elev + forest}. For occupancy
this is the occupancy probability formula; for N-mixture the abundance
formula; for the cover hurdle the latent-presence formula.

Structured effects are written as terms inside the formula, the way
\code{lme4}, \code{mgcv}, and \code{INLA} do: spatial fields \code{icar(graph = adj)},
\code{bym2(graph = adj)}, \code{gp(lon, lat)}, \code{spde(lon, lat)}; random effects
\code{re(group)}; temporal fields \code{temporal(time)}; spatially varying
coefficients \code{svc(lon, lat, indices = ...)}; community latent factors
\code{latent(k)}. A term enters whichever linear predictor it is written in
(occupancy \code{formula} or \code{detection}). To share one realization across
both predictors, tag the term with \code{id = "u"} and write \code{copy("u")} in
the other formula.

Random effects also accept \code{lme4} bar syntax as shorthand for \code{re()}:
\code{(1 | g)} is \code{re(g)}, \code{(x | g)} is a correlated random intercept and
slope \code{re(g, type = "slope", covariate = x)}, and \code{(x || g)} drops the
correlation (\code{correlated = FALSE}). Use \code{re()} directly for AR1/RW
structures or other options.}

\item{data}{data frame of site-level covariates with \code{nrow(data) == nrow(y)}.}

\item{family}{a \code{tobs_family} object (see \code{\link[=obs_family]{obs_family()}} and the concrete
constructors \code{\link[=occu]{occu()}}, \code{\link[=abun]{abun()}}, \code{\link[=cover]{cover()}}, ...).}

\item{detection}{detection-process formula, e.g. \code{~ observer + effort}.
Family-dependent: required for \code{\link[=occu]{occu()}} and \code{\link[=abun]{abun()}}, ignored for
\code{\link[=jsdm]{jsdm()}} and (currently) \code{\link[=cover]{cover()}}.}

\item{y}{response. Shape depends on family:
\itemize{
\item \code{\link[=occu]{occu()}} — N x J detection-history matrix.
\item \code{\link[=abun]{abun()}} — N x J integer count matrix.
\item \verb{ms_*} — S x N x J array.
\item \code{\link[=cover]{cover()}} — length-N vector of cover proportions in [0, 1].
}}

\item{visit_data}{optional visit-level detection covariates. Accepts
either:
\itemize{
\item a named list of \verb{[n_sites, max_visits]} matrices (the shape returned by
\code{tobs_data()} in \code{det.covs}) — flattened internally to a long data
frame in site-major order;
\item a data frame with \code{nrow(y) * ncol(y)} rows in site-major order.
}

When \code{visit_data} is provided without a \code{"formula"} attribute, the
\code{detection} argument is interpreted as the visit-level detection
formula and the site-level detection design matrix is an intercept
only. To split visit-level and site-level detection covariates
(e.g. visit-level effort plus site-level observer category), pass a
long data frame with \code{attr(visit_data, "formula") = ~ effort} and use
\code{detection = ~ observer} for the site-level terms.}

\item{engine}{inference engine: \code{"auto"}, \code{"laplace"}, \code{"nested_laplace"},
or \code{"nuts"}. \code{"auto"} chooses the family's \code{default_engine}.}

\item{approx}{marginal approximation for the Laplace path:
\code{"gaussian_laplace"} (default) or \code{"simplified_laplace"} (skew-corrected
marginals via the SLA path).}

\item{priors}{optional prior specification. For occupancy families fit
with \code{engine = "laplace"}, pass a list or \code{\link[=occu_priors]{occu_priors()}} object to
set weakly-informative quadratic priors on the fixed-effect
coefficients (defaults pull the detection intercept toward
\code{p = 0.5} and break the psi-p identifiability ridge at small \code{J}).
Pass \code{priors = FALSE} to disable the default prior and recover the
historical unpenalised MAP behavior. For NUTS, this is forwarded
to the underlying tulpa engine.}

\item{control}{list of low-level controls (\code{n_threads}, \code{max_iter}, \code{tol},
etc.).}

\item{...}{family-specific named arguments forwarded to the underlying
engine builder.}
}
\value{
An object of class \code{c("tobs_fit", "<family>_fit", "tulpa_fit")}.
}
\description{
Unified entry point for occupancy, abundance, distance, removal, and related
models that share the latent-state-plus-imperfect-detection generative
template. The specific model is chosen via the \code{family} argument; the engine
(Laplace / nested Laplace / NUTS) via \code{engine}.
}
\examples{
\dontrun{
# Single-season occupancy
fit <- tobs(
  formula   = ~ elev,
  data      = sites,
  family    = occu(),
  detection = ~ effort,
  y         = y_matrix
)
}

}

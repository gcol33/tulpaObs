% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_cover_hurdle.R
\name{simulate_cover}
\alias{simulate_cover}
\title{Simulate cover-hurdle data (lognormal positive part)}
\usage{
simulate_cover(
  N = 200L,
  beta_occ = c(-0.5, 0.8),
  beta_pos = c(-1, 0.3),
  sigma_pos = 0.4,
  spatial_range = NULL,
  spatial_var = 1,
  seed = NULL
)
}
\arguments{
\item{N}{Number of sites (default 200).}

\item{beta_occ}{Length-2 occurrence coefficients (intercept, slope on x).}

\item{beta_pos}{Length-2 log-cover coefficients (intercept, slope on x).}

\item{sigma_pos}{Lognormal residual standard deviation (default 0.4).}

\item{spatial_range}{Length scale of the exponential spatial field
(in unit-square distance). \code{NULL} disables the spatial layer.}

\item{spatial_var}{Marginal variance of the spatial field (default 1).}

\item{seed}{Optional integer seed.}
}
\value{
A list with:
\describe{
\item{data}{A data frame with \code{cover}, covariate \code{x}, and \code{lon}, \code{lat}.}
\item{y}{\code{data$cover} (length-N numeric vector, for passing to \code{tobs()}).}
\item{coords}{An N x 2 matrix of coordinates.}
\item{truth}{A list with \code{beta_occ}, \code{beta_pos}, \code{sigma_pos},
\code{p}, \code{mu}, \code{occur}, \code{spatial_occ}, \code{spatial_pos}.}
}
}
\description{
Generates synthetic data matching the \code{cover(positive = "lognormal")}
generative model used in Phase 1a:
}
\details{
\describe{
\item{eta_occ}{\verb{= X \%*\% beta_occ + spatial_occ}}
\item{eta_pos}{\verb{= X \%*\% beta_pos + spatial_pos}}
\item{p}{= plogis(eta_occ)}
\item{occur}{~ Bernoulli(p)}
\item{log_cover}{~ Normal(eta_pos, sigma_pos^2) | occur = 1}
\item{cover}{= exp(log_cover) when occur = 1, else 0}
}

Both layers share the same design matrix by default (one continuous
covariate plus an intercept). When \code{spatial_range} is supplied, a simple
exponential-kernel Gaussian field on a random unit-square coordinate set
is added to both linear predictors; the two layers see independently
simulated draws of that field (matching the Phase 1a separate-fit story;
the shared-field model is Phase 1c).
}
\examples{
sim <- simulate_cover(N = 200, seed = 1)
head(sim$data)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ms_distance.R
\name{simulate_ms_distance}
\alias{simulate_ms_distance}
\title{Simulate community (multispecies) binned distance-sampling data}
\usage{
simulate_ms_distance(
  n_species = 10,
  N = 100,
  cutpoints = c(0, 25, 50, 75, 100),
  transect = c("line", "point"),
  key = c("halfnorm", "hazard"),
  shape = 0,
  n_abund_covs = 1,
  n_det_covs = 0,
  mu_lambda = NULL,
  mu_sigma = NULL,
  sd_lambda = 0.4,
  sd_sigma = 0.2,
  n_factors = 0,
  load_sd = 0.5,
  field = NULL,
  seed = NULL
)
}
\arguments{
\item{n_species}{Number of species (default 10).}

\item{N}{Number of sites (default 100).}

\item{cutpoints}{Distance-bin edges (default \code{c(0, 25, 50, 75, 100)}).}

\item{transect}{Transect geometry: \code{"line"} (default) or \code{"point"}.}

\item{key}{Detection function: \code{"halfnorm"} (default) or \code{"hazard"}.}

\item{shape}{Hazard-rate log-shape, shared across species (ignored under the
half-normal key). Default 0.}

\item{n_abund_covs, n_det_covs}{Number of abundance / detection-scale
covariates (default 1 and 0).}

\item{mu_lambda}{Community-mean abundance coefficients on the log scale.
Default \code{c(log(30), rep(0.4, n_abund_covs))}.}

\item{mu_sigma}{Community-mean detection-scale coefficients on the log scale.
Default \code{c(log(40), rep(0, n_det_covs))}.}

\item{sd_lambda, sd_sigma}{Per-coefficient community SDs. Default 0.4 and 0.2.}

\item{n_factors}{If \verb{> 0}, add \code{n_factors} per-site latent factors with
per-species loadings to \verb{log lambda} (the lfMsDS truth). Default 0.}

\item{load_sd}{SD of the factor loadings (default 0.5).}

\item{field}{Optional length-\code{N} shared spatial field added to every species'
\verb{log lambda}. When given alongside \code{n_factors > 0} the loadings are centred
across species, so the field owns the shared spatial mean.}

\item{seed}{Optional random seed.}
}
\value{
A list with \code{y}, \code{data}, \code{species}, \code{cutpoints}, and \code{truth}
(community means / SDs, per-species coefficients, \code{lambda}, \code{sigma}, and --
with factors -- the \code{loadings}, \code{factors} and the implied residual
correlation \code{cor_res}). The latent \code{N} is drawn inside the shared C++
simulator and is not returned.
}
\description{
Per-species binned distance sampling with Gaussian community hyperpriors:
\code{beta_lambda_s ~ N(mu_lambda, diag(sd_lambda^2))},
\code{beta_sigma_s ~ N(mu_sigma, diag(sd_sigma^2))}, then
\verb{N_\{s,i\} ~ Poisson(lambda_\{s,i\})} and the detected individuals allocated to
distance bins by the half-normal detection function. The returned \code{y} is a 3D
array \verb{[n_sites x n_bins x n_species]} suitable for \code{\link[=tobs]{tobs()}} with
\code{\link[=ms_distance]{ms_distance()}}.
}

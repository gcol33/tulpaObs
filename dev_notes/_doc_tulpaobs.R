# Regenerate roxygen / NAMESPACE / Rd for tulpaObs (new export simulate_ms_abun;
# ms_abun status flip is code-only). document() load_all's, so this also
# confirms the package compiles + loads with the ms_abun wiring.
devtools::document("C:/Users/Gilles Colling/Documents/dev/tulpaObs")
cat("=== tulpaObs document done ===\n")

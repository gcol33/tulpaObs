# AGENTS.md

See `CLAUDE.md` for the full roster and rules. This file exists only so agent
tooling that looks for `AGENTS.md` by convention finds the same guidance --
it carries no content of its own, so it cannot drift out of sync.

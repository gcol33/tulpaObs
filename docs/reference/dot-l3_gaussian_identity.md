# Third derivative of Gaussian log-likelihood (identity link)

Always zero – the SLA correction reduces to the Laplace approximation
for Gaussian likelihoods. Required regression test.

## Usage

``` r
.l3_gaussian_identity(eta)
```
